# MERIDIAN — HEALTH CONTEXT INSIGHT INTEGRATION PHASE 3

We are implementing Phase 3: safe Health Context integration into Meridian insights.

Important context:
The real app lives in:

.migration-backup/

Health Context fields already exist in Profile and Supabase.

Goal:
Use Health Context to improve insight relevance and wording, without turning Meridian into a prescription engine.

This task should use Health Context only for:
- context
- safer wording
- relevance
- avoiding generic advice
- adjusting action step language

Do NOT calculate:
- protein grams
- hydration liters
- calories
- macros
- supplement dosages
- medical dosing
- exact training prescriptions

Modify ONLY:
.migration-backup/src/app/api/insight/route.ts

Do not touch:
- dashboard
- labs
- profile
- onboarding
- upload flow
- OCR routes
- confirm route
- protocol
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create files.
TypeScript strict.

CURRENT STATE:
The insight route currently fetches profile fields:
- biological_profile
- user_profile
- medications

Now add selected Health Context fields:
- activity_level
- training_days
- body_goal_phase
- diet_pattern
- weight_kg

Do NOT add height_cm in this phase.

TASK:

1. Extend profile SELECT

Find the profile SELECT in:

.migration-backup/src/app/api/insight/route.ts

Extend it from something like:

biological_profile, user_profile, medications

to include:

activity_level,
training_days,
body_goal_phase,
diet_pattern,
weight_kg

Do not include height_cm.

2. Add local HealthContext interface

Add a local interface near existing interfaces:

interface HealthContext {
  activityLevel: string | null
  trainingDays: number | null
  bodyGoalPhase: string | null
  dietPattern: string | null
  weightKg: number | null
}

This is route-local only.
Do not change database types.

3. Extract Health Context safely

After profile is loaded, build:

const healthContext: HealthContext = {
  activityLevel: typeof profile.activity_level === 'string' ? profile.activity_level : null,
  trainingDays: typeof profile.training_days === 'number' ? profile.training_days : null,
  bodyGoalPhase: typeof profile.body_goal_phase === 'string' ? profile.body_goal_phase : null,
  dietPattern: typeof profile.diet_pattern === 'string' ? profile.diet_pattern : null,
  weightKg: typeof profile.weight_kg === 'number' ? profile.weight_kg : null,
}

Do not crash if any field is missing or null.

4. Update buildSystemPrompt signature

Update buildSystemPrompt() to accept:

healthContext: HealthContext

Update its call site to pass healthContext.

5. Add readable context labels

Inside insight/route.ts, create small helper maps or helper function to convert raw values into readable labels.

Examples:

activity_level:
sedentary → sedentary
light → lightly active
moderate → moderately active
active → active
athletic → athletic

body_goal_phase:
fat_loss → fat loss
maintenance → maintenance
muscle_gain → muscle gain
recomposition → recomposition
performance → performance
wellness → general wellness

diet_pattern:
no_restriction → no specific restriction
balanced → balanced
high_protein → high-protein
vegetarian → vegetarian
vegan → vegan
mediterranean → Mediterranean
low_carb → low-carb
keto → keto
other → other

6. Extend USER CONTEXT block

Inside buildSystemPrompt(), add a compact Health Context section.

Only include non-null fields.

Example output:

HEALTH CONTEXT:
- Reported activity level: moderately active
- Reported training frequency: 4 days/week
- Reported body goal: recomposition
- Reported diet pattern: high-protein
- Weight context: available internally, but never mention the number to the user

Important:
If all fields are null, do not include the Health Context section or show “null”.

Never include weight_kg as a number in the prompt-visible context.
Only include a safe line like:
- Weight context: available internally, but never mention the number to the user

If weight_kg is null, omit it completely.

7. Add Rule 17 to ABSOLUTE RULES

Add:

17. HEALTH CONTEXT: Use Health Context only when it directly improves relevance, wording, or action-step clarity for the dominant signal. Do not enumerate every context field. Do not mention the user's weight or height as a number. Do not generate numeric protein targets, hydration volumes, calorie targets, macro targets, supplement dosages, medication dosing, or exact training prescriptions based on Health Context alone. If Health Context is missing or irrelevant, ignore it.

8. Safe usage examples

Add examples under Rule 17.

Good:
“Since your diet is already high-protein, stick to your usual protein portions today rather than adding extra.”

Good:
“Given that you train regularly, keep movement easy today instead of adding a hard session.”

Good:
“Since your goal is recomposition, keep protein steady and focus today on hydration.”

Bad:
“Eat 143 grams of protein.”
“Drink 3.2 liters of water.”
“Train in Zone 2 for exactly 45 minutes.”
“Because your weight is X...”

9. Do not change insight schema

Do NOT add:
- boundary
- context_note
- health_context
- any new output field

The output JSON schema must remain:
- headline
- status
- cause
- action_steps
- trust_line
- block_color
- logic_trace

10. Do not change guardrails

Do not change:
- hallucination guard
- forbidden word guard
- insight_unavailable behavior
- action step max count
- safety alert behavior

11. Do not change Dashboard

Dashboard boundary note stays deterministic.
No dashboard edits.

12. Build and QA

Run:

cd .migration-backup && npm run build

QA checklist:
- Build passes.
- insight/route.ts compiles.
- Profile SELECT includes activity_level, training_days, body_goal_phase, diet_pattern, weight_kg.
- height_cm is not added.
- Health Context is omitted if all values are null.
- Health Context does not show null values.
- weight_kg number is never included in prompt text.
- Rule 17 exists.
- Rule 17 bans numeric protein/hydration/calorie/macro/supplement/training prescriptions.
- Safe examples are present.
- JSON schema unchanged.
- Dashboard unchanged.
- Guardrails unchanged.
- Decision Engine unchanged.
- Safety Engine thresholds unchanged.

Do not commit.
Do not push.
Do not publish.

OUTPUT REQUIRED:
Return:
1. Summary of Health Context integration
2. Exact file changed
3. Which fields were added
4. Confirmation height_cm was intentionally excluded
5. Confirmation no numeric targets are allowed
6. Confirmation JSON schema unchanged
7. Build result
8. QA checklist result
9. Confirmation no unrelated systems were touched