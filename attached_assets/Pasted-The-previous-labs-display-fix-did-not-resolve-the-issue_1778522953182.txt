The previous labs display fix did not resolve the issue in production.

Current behavior after latest changes:
- I uploaded and confirmed 2 lab PDFs.
- Upload success appears to work.
- Dashboard still shows “Upload your first lab PDF”.
- /labs/history still shows “Coming soon”.
- Saved labs/biomarkers are not visible anywhere.

Do not edit code yet.
Do not commit.
Do not push.
Do not publish.

Diagnose the real production issue.

Required investigation:

1. Confirm whether the latest local code includes the previous fix:
   - insight/route.ts no longer selects safety_status
   - dashboard/page.tsx logs insight API errors
   - labs/upload/page.tsx redirects to /dashboard after confirm

2. Confirm whether those changes are committed and pushed to GitHub.
   Run:
   git status -sb
   git log --oneline -5

3. Confirm whether Vercel production is on the latest commit.

4. Inspect /api/insight behavior:
   - Determine what /api/insight returns for a logged-in user who has uploaded labs.
   - If possible, log or report the exact JSON response:
     success, state, error, biomarkers count.

5. Inspect confirmed biomarkers in Supabase:
   - Query biomarkers_static for the current logged-in user_id.
   - Count rows.
   - Show the most recent 5 rows with:
     user_id, biomarker_name/name, value, unit, collected_at, created_at
   - Confirm whether rows exist for the same user_id currently logged into the app.

6. Inspect upload confirm route:
   - Confirm whether it inserts rows into biomarkers_static.
   - Confirm whether inserted user_id comes from the authenticated user or from client payload.
   - Confirm whether insert returns success even if rows are not actually visible to the current user.

7. Inspect dashboard:
   - Confirm whether dashboard calls /api/insight with the correct user_id.
   - Confirm whether dashboard user_id matches the rows in biomarkers_static.
   - Confirm whether dashboard still treats an API error as no_data.

8. Inspect /labs/history:
   - Confirm whether it is still a hardcoded Coming Soon placeholder.
   - Confirm whether it queries biomarkers_static at all.

Report only:
- What is the exact root cause?
- Are biomarkers being inserted?
- Are they inserted under the right user?
- Is /api/insight failing?
- Is dashboard hiding the failure?
- Is Vercel running the latest commit?
- What is the smallest safe fix?

Do not modify files until I approve.