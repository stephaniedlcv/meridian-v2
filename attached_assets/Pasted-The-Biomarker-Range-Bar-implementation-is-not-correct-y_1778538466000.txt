The Biomarker Range Bar implementation is not correct yet.

Current issue:
1. The range bar is either missing or not placed correctly.
2. The colors do not match Meridian’s status system.
3. The bar must appear only inside individual biomarker rows, not on panel summary cards.
4. The result dot/circle must clearly show where the user’s value falls.

Do NOT sync/publish.
Fix this carefully.

Modify ONLY if needed:

1. .migration-backup/src/app/labs/history/page.tsx
2. .migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- OCR routes
- confirm route
- canonical dictionary
- dashboard
- Supabase clients
- database types
- middleware
- onboarding
- NavBar
- package.json
- artifacts/
- dependencies

Required visual logic:

Status colors must be exactly:

Optimal:
- teal/cyan
- use #2DD4BF or existing Meridian teal

Watch:
- yellow/amber
- use #FCD34D

Attention:
- orange
- use #FB923C

Critical:
- red
- use #F87171

Do not use orange for Watch.
Do not use red for Attention.
Do not use teal for Watch/Attention/Critical.
Do not invent new colors.

Where the range bar should appear:

1. /labs/history:
When a panel is expanded, each individual numeric biomarker row should show:
- biomarker name
- value + unit
- state badge
- reference range text
- compact range bar with user value dot

2. /labs/upload:
Only individual biomarkers in the “Needs Attention” section should show the range bar if range data exists.

Do NOT show range bars on:
- panel summary cards
- year/month/date headers
- Recent Lab Snapshot summary
- collapsed panel cards
- qualitative urine results
- rows without numeric value
- rows without reference_range_min/reference_range_max

Required range bar behavior:

1. Show the bar only if:
- value is numeric
- reference_range_min is numeric
- reference_range_max is numeric
- reference_range_min < reference_range_max

2. Do NOT require optimal_range_min/optimal_range_max.
If optimal range exists:
- show a teal highlighted optimal segment inside the full bar.
If optimal range does not exist:
- show a simple muted reference bar.

3. Dot position:
percent = ((value - reference_range_min) / (reference_range_max - reference_range_min)) * 100

Clamp:
- below min → 0%
- above max → 100%

4. The dot/circle color must use the biomarker state:
- Optimal → teal
- Watch → yellow
- Attention → orange
- Critical → red

5. The bar base should be subtle/muted, not neon.
6. The dot must be clearly visible on dark background.
7. The bar must be compact and mobile-friendly.
8. Keep labels small and muted:
- Low
- Optimal
- High
or
- Ref min
- Optimal
- Ref max

Do not overcrowd the row.

Important examples:
- HDL Cholesterol 47.1 mg/dL Attention should have an orange dot.
- A Watch marker should have a yellow dot.
- An Optimal marker should have a teal dot.
- A Critical marker should have a red dot.
- If HDL has reference range data, it must show a range bar.
- If reference range data is missing, do not fake the range.

First diagnose and report:
1. Does each row have reference_range_min/reference_range_max?
2. Is the query selecting those fields?
3. Is the component rendered inside the biomarker row?
4. Are status colors mapped correctly?
5. Are bars hidden because range data is missing?

Then fix.

After editing:
1. Report exact files changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/history
   /labs/upload
5. Confirm build passes.
6. Confirm color mapping:
   Optimal teal
   Watch yellow
   Attention orange
   Critical red
7. Confirm where the bar appears and where it is intentionally hidden.
8. Do not commit.
9. Do not push.
10. Do not publish to Replit.