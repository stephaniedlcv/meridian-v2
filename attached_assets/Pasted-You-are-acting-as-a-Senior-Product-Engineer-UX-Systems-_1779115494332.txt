You are acting as a Senior Product Engineer, UX Systems Architect, and Next.js 14 Frontend Lead for Meridian.

Your task is to implement a production-safe, scalable “Landing Experience CMS Layer” for Meridian’s landing page.

IMPORTANT:
This is NOT a generic admin panel feature.

The goal is to decouple:

* emotional/branding content
  FROM
* frontend code deployments.

Meridian’s landing experience is evolving into a cinematic biological operating system introduction, and the team must be able to iterate quickly on:

* messaging
* atmosphere
* videos
* branding
* visual mood
  without requiring constant frontend code edits or redeploys.

---

## CURRENT PROBLEM

Right now:

* landing hero content is hardcoded
* changing copy requires code edits
* changing video requires touching frontend assets
* experimenting with messaging is slow
* emotional iteration is bottlenecked by development

We need:
a lightweight internal “Experience Manager” system.

---

## GOAL

Create a lightweight admin-controlled Landing Experience configuration system.

This should allow Meridian admins to dynamically manage:

* landing hero video
* poster/fallback image
* headline
* subcopy
* CTA labels
* logo/doodle asset
* atmospheric overlay intensity
* gradient/background mode

WITHOUT touching frontend code.

---

## TECH STACK

Project stack:

* Next.js 14 App Router
* TypeScript
* Supabase
* Tailwind
* Existing Meridian design system

DO NOT:

* restructure the entire app
* add heavy CMS dependencies
* introduce unnecessary complexity
* break existing auth architecture

This should be:

* lightweight
* scalable
* production-safe
* admin-controlled
* future extensible

---

## DATABASE REQUIREMENTS

Create a new Supabase table:

landing_experience

Suggested fields:

id (uuid)
is_active (boolean)
hero_video_url (text)
mobile_video_url (text)
poster_image_url (text)
headline (text)
subcopy (text)
primary_cta_label (text)
secondary_cta_label (text)
logo_variant_url (text)
background_theme (text)
overlay_opacity (numeric)
ambient_mode (text)
created_at
updated_at

Requirements:

* support a single active configuration
* future support for multiple landing “modes”
* production-safe defaults
* nullable-safe handling

---

## ADMIN PANEL REQUIREMENTS

Add a new admin section:

Experience Manager
OR
Landing Experience

This section should allow:

* editing all landing content
* uploading/replacing hero video
* replacing poster image
* editing text inline
* updating CTA labels
* swapping logo/doodle assets
* adjusting overlay intensity
* previewing changes before saving

IMPORTANT:
This should feel:

* premium
* minimal
* clean
* aligned with Meridian aesthetics

NOT:

* enterprise CMS
* cluttered dashboard
* generic admin tooling

---

## LANDING PAGE REQUIREMENTS

Refactor the landing page so ALL hero content is dynamically loaded from the landing_experience configuration.

This includes:

* video source
* poster image
* headline
* subcopy
* CTA labels
* logo asset
* atmosphere/background config

Requirements:

* graceful loading states
* fallback config if DB unavailable
* no hydration mismatch
* optimized video loading
* mobile-safe behavior
* autoplay muted loop
* no performance regression

---

## VIDEO SYSTEM REQUIREMENTS

The landing video is a cinematic atmospheric loop.

Requirements:

* autoplay
* muted
* loop
* playsInline
* optimized for mobile
* smooth loading
* fallback poster image
* graceful degradation for slow connections

IMPORTANT:
The landing hero video should feel:

* atmospheric
* cinematic
* subtle
* premium

NOT:

* laggy
* overprocessed
* heavy
* performance-destructive

---

## VISUAL SYSTEM REQUIREMENTS

Maintain Meridian visual philosophy:

* deep biological dark backgrounds
* soft teal/cyan ambient glow
* restrained glassmorphism
* elegant serif typography
* cinematic calm
* emotional grounding

Avoid:

* CMS clutter
* dashboard-looking controls
* enterprise admin aesthetics

---

## FUTURE EXTENSIBILITY

Architect this system cleanly so future support can be added for:

* seasonal landing themes
* launch campaigns
* wearable launch states
* A/B headline testing
* dynamic onboarding modes
* animated logo variants
* adaptive landing experiences

WITHOUT requiring major rewrites later.

---

## IMPORTANT ENGINEERING RULES

1. Prioritize performance over visual gimmicks.
2. Avoid unnecessary rerenders.
3. Avoid large video loading issues.
4. Use lazy loading where appropriate.
5. Ensure mobile-first behavior.
6. Maintain smooth cinematic feel.
7. Preserve existing auth architecture.
8. Do not break onboarding flows.
9. Keep implementation modular and clean.
10. Preserve Meridian premium feel at all times.

---

## DELIVERABLES

Implement:

1. Supabase schema changes
2. Admin panel “Experience Manager”
3. Dynamic landing configuration loader
4. Dynamic hero rendering system
5. Video upload/replacement flow
6. Fallback handling
7. Mobile-safe optimized video behavior
8. Preview/save workflow
9. Clean modular architecture

Return:

* SQL schema
* affected files
* implementation summary
* architectural reasoning
* any future recommendations
