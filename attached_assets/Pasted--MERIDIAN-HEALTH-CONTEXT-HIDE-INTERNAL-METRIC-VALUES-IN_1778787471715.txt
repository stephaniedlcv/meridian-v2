# MERIDIAN — HEALTH CONTEXT HIDE INTERNAL METRIC VALUES IN READ VIEW

Small Health Context polish.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:

.migration-backup/src/app/profile/page.tsx

Do not touch:
- database.ts
- dashboard
- labs
- onboarding
- upload flow
- OCR routes
- insight route
- intelligence route
- protocol
- Supabase clients
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

CURRENT ISSUE:
Health Context read-only view displays both user-friendly imperial values and internal metric values:

Height: 5 ft 4 in · 163.8 cm
Weight: 143.3 lb · 65.0 kg

This is too technical for the normal user-facing Profile view.

PRODUCT RULE:
Meridian should make this easy for the user.

The database can store:
- height_cm
- weight_kg

But the Profile read-only UI should show only the user-facing values:
- Height in ft/in
- Weight in lb

Metric values should remain internal and hidden from the normal Profile read-only view.

TASK:
Update only the read-only display formatting for Health Context height and weight.

REQUIREMENTS:

1. Height read-only display

Change from:

5 ft 4 in · 163.8 cm

to:

5 ft 4 in

2. Weight read-only display

Change from:

143.3 lb · 65.0 kg

to:

143.3 lb

3. Internal storage unchanged

Do NOT change:
- height_cm storage
- weight_kg storage
- conversion helpers used for saving
- unit toggles in edit mode
- database schema
- database types

4. Edit mode unchanged

Keep edit mode user-friendly:
- Height can still be entered as ft/in or cm
- Weight can still be entered as lb or kg
- Values still save internally as cm/kg

5. Optional helper names

If displayHeight/displayWeight currently include metric values, either:
- update them to return only imperial values for read view
OR
- create separate read-only helpers like displayHeightForUser and displayWeightForUser

Choose the smallest clean change.

6. Build and QA

Run:

cd .migration-backup && npm run build

QA checklist:
- Profile loads.
- Health Context read-only Height shows only ft/in.
- Health Context read-only Weight shows only lb.
- Edit mode still allows ft/in and cm for height.
- Edit mode still allows lb and kg for weight.
- Saving height still stores height_cm.
- Saving weight still stores weight_kg.
- Training days validation still works.
- Activity / Goal / Diet unchanged.
- Build passes.

Do not commit.
Do not push.
Do not publish.

OUTPUT REQUIRED:
Return:
1. Exact change made
2. Exact file changed
3. Build result
4. QA checklist result
5. Confirmation no unrelated systems were touched