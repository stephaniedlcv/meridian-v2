# MERIDIAN — INSIGHT GUARDRAIL PHASE 2 QA ONLY

Insight Guardrail Hardening Phase 2 was implemented.

This is QA ONLY.

Do NOT edit files.
Do NOT modify code.
Do NOT commit.
Do NOT push.
Do NOT publish.

Important context:
The real app lives in:

.migration-backup/

Scope:
Verify that failed insight guardrails now return insight_unavailable instead of showing broken or unsafe insight copy.

Inspect:
.migration-backup/src/app/api/insight/route.ts

Run:
cd .migration-backup && npm run build

QA CHECKLIST:

1. Build
Confirm:
- build passes
- zero TypeScript errors
- no new warnings

2. Hallucination guard

Confirm:
- validateMarkers failure now returns:
  state: 'insight_unavailable'
  insight: null
- generated insight is NOT returned
- logic_trace warning continuation was removed
- dominant_marker is preserved
- safety_alert is preserved

3. Forbidden word guard

Confirm:
- containsForbiddenWords failure now returns:
  state: 'insight_unavailable'
  insight: null
- generated insight is NOT returned
- no replace-with-*** behavior remains
- no user-facing field can render "***"
- dominant_marker is preserved
- safety_alert is preserved

4. Normal path

Confirm:
- if validateMarkers passes
- and containsForbiddenWords returns null
- the route still returns normal solved/safety_alert insight
- action_steps are still sliced to max 3
- JSON schema is unchanged

5. Scope safety

Confirm no changes to:
- Dashboard
- Labs
- Profile
- Onboarding
- Decision Engine
- Safety Engine thresholds
- Supabase clients
- Database types
- package.json

6. Health Context

Confirm Health Context fields are still not added to the insight prompt:
- height_cm
- weight_kg
- activity_level
- training_days
- body_goal_phase
- diet_pattern

7. Remaining risks

Report any remaining known risks:
- false positives from validateMarkers
- lack of retry
- boundary field not yet implemented

OUTPUT REQUIRED:
1. Executive QA summary
2. Files inspected
3. Build result
4. Guardrail behavior verification
5. Remaining risks, if any
6. Whether Guardrail Phase 2 can be considered closed
7. Confirmation:
   “No files were changed.”