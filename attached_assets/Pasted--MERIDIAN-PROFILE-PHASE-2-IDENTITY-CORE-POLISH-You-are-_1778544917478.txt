# MERIDIAN — PROFILE PHASE 2: IDENTITY CORE POLISH

You are continuing inside the working Meridian Next.js app.

Important context:
The real app lives in:

.migration-backup/

Vercel production is working because Root Directory is set to .migration-backup.

Do NOT restructure the repo.
Do NOT move files out of .migration-backup.
Do NOT touch root package files.
Do NOT touch Vercel settings.
Do NOT touch artifacts/.
Do NOT publish to Replit.

Current state:
The /profile page now exists and works.
It has:
- Biological Identity hero
- M avatar placeholder
- Upload photo action
- Identity card
- Health Focus card
- Connected Sources card
- Account card
- Sign out
- Bottom NavBar

The page is clean and functional, but it still feels too much like a standard premium settings screen. We want Phase 2 to make it feel more unique, futuristic, and distinctly Meridian.

TASK:
Upgrade /profile into a more premium “Meridian Identity Core” experience.

Modify ONLY:

.migration-backup/src/app/profile/page.tsx

Do not touch:
- dashboard
- labs
- history
- upload
- OCR routes
- confirm route
- canonical dictionary
- Supabase clients
- database types
- middleware
- onboarding
- NavBar
- package.json
- artifacts/
- dependencies

CRITICAL RULES:
- Inline styles only.
- No Tailwind.
- No new dependencies.
- No new files.
- TypeScript strict.
- Preserve all current working functionality.
- Keep auth guard working.
- Keep photo upload/local preview behavior if already implemented.
- Keep sign out working if already implemented.
- Do not assume database fields that do not exist.
- Do not break build.

DESIGN GOAL:
Make Profile feel like a futuristic biological identity center, not a normal settings page.

Think:
- biological passport
- health identity core
- personal OS profile
- premium medical intelligence
- dark glass interface
- orbital / signal / identity system

It should feel warmer, more futuristic, more memorable, and more “Meridian.”

PHASE 2 VISUAL UPGRADES:

1. HERO — make it more cinematic

Current hero is clean but simple.
Upgrade it into a stronger identity module.

Add:
- subtle orbital rings around avatar using CSS only
- soft cyan/teal glow behind the avatar
- small status row/chips under the name:
  - “Profile active”
  - biological profile if available
  - health focus mode if available
- if values are missing, use elegant placeholders:
  - “Biology pending”
  - “Focus not set”
- do NOT show ugly dashes like “—” as the main visual state

Display name logic must remain:
1. preferred_name if available
2. full_name if available
3. “Your Profile”
Never use email prefix as display name.

2. EMPTY DATA STATES — make them feel intentional

Current cards show missing values as “—”.
Replace that with polished empty states.

Examples:
Instead of:
Display name: —

Use:
Display name
“Not set yet”
Button or small link: “Add name”

Instead of:
Biological profile: —

Use:
Biological profile
“Pending calibration”
Microcopy: “Used to personalize lab interpretation.”

Keep the page feeling complete even if the user has not entered data yet.

3. BIOLOGICAL PASSPORT CARD

Upgrade the Identity card into a “Biological Passport” card.

Show compact rows:
- Display name
- Email
- Biological profile
- Meridian mode / health focus

Add a tiny passport-like header:
“BIOLOGICAL PASSPORT”
or
“IDENTITY SIGNALS”

Make the card feel less like a form and more like a personal data capsule.

Use:
- small labels
- strong values
- subtle dividers
- status chips
- glass border
- clean spacing

4. HEALTH FOCUS — make it more visual

Current Health Focus card is mostly text.
Upgrade it to feel like a mode selector/status module.

If user_profile exists:
- show large mode label:
  Wellness / Optimization / Performance / Specific condition / Getting started
- show short explanation
- show 2–3 signal chips, for example:
  - Labs
  - Recovery
  - Trends

If user_profile is missing:
- show:
  “No health focus set”
  “Choose how Meridian should personalize your insights.”
  Button: “Set focus”

Keep Adjust button.

5. DATA CONSTELLATION

Upgrade Connected Sources from list cards into a more futuristic “Data Constellation.”

Do not overcomplicate with real SVG/animation.
Use CSS-only node cards.

Each source should feel like a signal node:
- Labs: Connected
- Wearables: Coming soon / Ready
- Genetic Data: Coming soon
- Feedback Loop: Coming soon

Suggested visual:
- small glowing dot
- node title
- short description
- status pill
- subtle connecting line or background grid/orbit if safe

Copy:
“Meridian gets smarter as your biological signals connect.”

6. PERSONALIZATION MATRIX

Add a new premium card called:

“Personalization Matrix”

This card explains how Meridian uses profile context.

Rows/nodes:
- Biological profile
- Age context
- Lab history
- Wearable signals
- Medication context
- Feedback loop

Each row should have a status:
- Active
- Pending
- Coming soon

Purpose:
Help users understand why completing profile data matters.

Keep it educational and non-medical:
“Used to personalize insight priority.”
“Used to improve biological context.”
“Used to refine trend interpretation.”

7. ACCOUNT / CONTROL LAYER

Upgrade Account card so it feels less generic.

Rename or visually present as:
“Control Layer”

Include:
- App preferences — Soon
- Data sources — Soon
- Privacy controls — Soon
- Sign out

Sign out can remain red, but keep it elegant.

8. PHOTO AREA

If local photo preview is implemented:
- preserve it
- show uploaded photo cleanly inside the avatar ring
- keep Change/Remove photo actions

If photo is only local preview:
- show small helper:
  “Photo preview only. Storage connection pending.”

Do not attempt Supabase Storage unless it already exists and is wired.

9. MICROCOPY

Use premium Meridian tone:
- warm
- futuristic
- clear
- not cheesy
- not clinical in a cold way

Examples:
“Your biological identity helps Meridian personalize your daily signal.”
“Complete your profile to improve context over time.”
“Meridian interprets. You decide.”

10. MOBILE SPACING

The current page gets close to the bottom NavBar.
Increase bottom padding so content is never covered.

Use at least:
paddingBottom: 180px
or whatever matches the current layout.

Make sure the final footer/account area is readable above the nav.

11. VISUAL POLISH

Use:
- dark background
- ambient orbs
- glass cards
- subtle teal/cyan borders
- gradient M avatar
- Fraunces for major identity elements
- Plus Jakarta Sans for UI/body
- soft shadows
- no heavy neon
- no emojis

Do not make it cluttered.
Do not make it look like iOS settings.
Do not make it look like a medical portal.

AFTER EDITING:
1. Report exact file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /profile
5. Confirm build passes.
6. Confirm:
   - hero is more premium/futuristic
   - missing data states no longer show ugly dashes
   - avatar placeholder still works
   - local photo preview still works if already implemented
   - display name does not use email prefix
   - connected sources look more like a data constellation
   - personalization matrix appears
   - sign out still works if previously wired
   - bottom nav does not cover content
7. Do not commit.
8. Do not push.
9. Do not publish to Replit.