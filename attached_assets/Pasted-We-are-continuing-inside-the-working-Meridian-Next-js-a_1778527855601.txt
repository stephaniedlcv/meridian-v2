We are continuing inside the working Meridian Next.js app.

Important context:
The real app lives in:

.migration-backup/

Vercel production is working because Root Directory is set to .migration-backup.

Do NOT restructure the repo.
Do NOT move files out of .migration-backup.
Do NOT touch root package files.
Do NOT touch Vercel settings.
Do NOT touch artifacts/.
Do NOT publish to Replit.

Current approved tasks:
1. Fix the dashboard false no-data state.
2. Polish the bottom navigation icons.

Modify ONLY these files if needed:

1. .migration-backup/src/app/dashboard/page.tsx
2. .migration-backup/src/components/NavBar.tsx

Do not touch:
- /labs/history
- /labs/upload
- onboarding files
- API routes
- Supabase client files
- database types
- middleware
- package.json
- artifacts/
- dependencies

---

TASK 1 — Dashboard false no-data state

Current issue:
The dashboard still says “Upload your first lab PDF” even when the user has saved biomarkers visible in /labs/history.

Product rule:
If the user has any saved biomarkers in biomarkers_static, the dashboard must NOT say they have no labs.

Expected behavior:
- If user has 0 biomarkers: show true no_data upload-first-lab state.
- If user has saved biomarkers but no current insight: show a “labs saved / calibrating” state instead.
- This state should reassure the user that Meridian has their data.

Suggested copy:
Headline:
Labs received

Status:
Meridian is building your biological baseline.

Cause:
Your saved lab history is now part of your Meridian profile. Daily insights will become more precise as newer labs, wearable signals, and feedback are added.

Action steps:
1. Review your Lab History
2. Upload newer labs if available
3. Connect wearables when ready

Trust line:
Based on your saved biomarkers · Meridian interprets, you decide.

Implementation guidance:
- Do not change /api/insight for this task.
- In dashboard/page.tsx, if /api/insight returns no_data, perform a lightweight fallback check against biomarkers_static for the logged-in user.
- If count > 0, render the calibrating/labs-saved state instead of upload-first-lab.
- If count = 0, keep the true no_data state.
- Keep this minimal and safe.

Do not hide real API errors as no_data if possible.

---

TASK 2 — Bottom Nav icon polish

Current issue:
The bottom nav icons look like demo/emoji icons. Home and Profile are not clearly visible.

Goal:
Make the bottom nav feel premium, futuristic, and consistent with Meridian.

Required behavior:
- Keep the same nav items and routes:
  - Home → /
  - Labs → /labs/upload
  - History → /labs/history
  - Profile → /profile
- Do not change routing behavior.

Icon requirements:
1. Home icon should be a mini Meridian “M”.
   - Use fontFamily: "var(--font-fraunces), serif"
   - Active state should use the premium white → cyan → teal gradient.
   - Inactive state should be muted but still visible.
   - Subtle glow when active.

2. Labs icon:
   - Replace emoji/demo icon with a simple inline SVG lab/flask/test-tube style icon.
   - No icon library.
   - No dependency.

3. History icon:
   - Replace emoji/demo icon with a simple inline SVG chart/history/trend style icon.
   - No icon library.
   - No dependency.

4. Profile icon:
   - Replace weak/invisible icon with a simple inline SVG user/circle icon.
   - Must be clearly visible in inactive and active states.

Visual requirements:
- Active item should have teal/cyan glow.
- Inactive items should be muted but readable.
- Nav background should keep premium glassmorphism.
- Do not use emoji icons.
- Do not make it gamer/neon/crypto.
- Keep it mobile-friendly.
- Ensure nav does not cover page content.

Rules:
- Minimal safe implementation.
- Visual/UI logic only.
- Do not add dependencies.
- Do not create files.
- Inline styles only.
- Do not commit.
- Do not push.
- Do not publish to Replit.

After editing:
1. Report every file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   - /
   - /dashboard
   - /labs/upload
   - /labs/history
   - /profile
5. Confirm build passes.
6. Explain how the dashboard now distinguishes true no_data from labs_saved/calibrating.
7. Explain how the nav icons were changed.