# MERIDIAN — TASK: Unified Labs Experience
# Move History inside Labs as Snapshot | History toggle

You are working on Meridian — a premium Health Intelligence System.

Stack:
- Next.js 14 App Router
- TypeScript
- Supabase
- Vercel
- Inline styles only

IMPORTANT:
- Real app lives inside `.migration-backup/`
- Do NOT move files outside `.migration-backup/`
- Do NOT restructure repo broadly
- Do NOT redesign the app
- Do NOT add dependencies
- Do NOT touch artifacts/
- Do NOT change Supabase schema
- Do NOT change OCR logic
- Do NOT change Safety Engine logic
- Do NOT change biomarker status logic
- Do NOT change database queries unless needed to reuse existing History data
- Do NOT touch onboarding/auth
- This is a UX/navigation architecture cleanup task

GOAL:
Unify the Labs experience so History is no longer a primary bottom-nav item.

Final bottom nav should be:

Home · Labs · Protocol · Profile

History should live inside Labs as an internal view/toggle:

Snapshot | History

This aligns Meridian’s product architecture:
- Home = current biological signal / daily intelligence
- Labs = evidence / current snapshot + historical archive
- Protocol = action layer
- Profile = identity/personalization

────────────────────
TASK 1 — REMOVE HISTORY FROM BOTTOM NAV
────────────────────

Update the bottom navigation config so it contains only:

1. Home
2. Labs
3. Protocol
4. Profile

Remove History from bottom nav only.

Do NOT delete the History page yet unless absolutely necessary.
Do NOT break any existing `/labs/history` route.
Do NOT remove History logic.

Expected:
- Nav has 4 items.
- Spacing feels less crowded.
- Protocol has more room.
- Active nav state still works.

────────────────────
TASK 2 — ADD SNAPSHOT | HISTORY TOGGLE INSIDE LABS
────────────────────

Inside the Labs page, add a premium internal toggle/chip control near the top, visually aligned with Meridian’s chip language.

Suggested placement:
- Under the Labs header/subtitle
- Before the latest collection snapshot card

Toggle labels:

Snapshot
History

Default view:
Snapshot

When Snapshot is active:
- show the existing current Labs page content:
  - recent lab snapshot
  - biomarker counts
  - Needs Attention
  - filters
  - current biomarker cards

When History is active:
- show existing Lab History content inside the Labs page:
  - grouped by year → month → date → panel
  - expandable panels
  - delete session logic
  - biomarker detail sheet behavior
  - safety behavior

Important:
This should feel like one Labs experience, not two separate pages pasted together.

────────────────────
TASK 3 — REUSE EXISTING HISTORY LOGIC SAFELY
────────────────────

Inspect:
- `.migration-backup/src/app/labs/upload/page.tsx`
- `.migration-backup/src/app/labs/history/page.tsx`
- `.migration-backup/src/components/NavBar.tsx`

Use the existing History logic as much as possible.

Acceptable approaches:
- Move shared history rendering into the Labs page
- Or extract reusable local components if needed
- Or import/reuse existing functions if safe

Do NOT create a broad architecture refactor.
Do NOT rewrite History from scratch.

Preserve:
- grouping
- panel expansion
- delete flow
- status counts
- range bars
- detail sheets
- Safety Engine behavior
- historical archive behavior

────────────────────
TASK 4 — ROUTE BEHAVIOR
────────────────────

Keep `/labs/history` working if it already exists.

Acceptable:
- `/labs/history` can redirect to `/labs/upload?view=history`
or
- remain as legacy route for now
or
- render the same history view

But bottom nav should no longer point to `/labs/history`.

If using query params:
- `/labs/upload?view=snapshot`
- `/labs/upload?view=history`

Make sure direct link to history still works if possible.

Do NOT break bookmarks unnecessarily.

────────────────────
TASK 5 — VISUAL REQUIREMENTS
────────────────────

The internal toggle should feel premium and Meridian-native.

Design style:
- glassy chips
- teal active state
- muted inactive state
- readable labels
- no heavy borders
- no neon
- no bulky segmented control
- consistent with existing Optimal/Watch/Attention chip language

Labs header should remain calm and readable.

Do NOT redesign biomarker cards in this task.
Do NOT change typography globally.

────────────────────
TASK 6 — COPY CLEANUP
────────────────────

Update copy if needed so Labs now represents both snapshot and archive.

Possible subtitle:
“Meridian is tracking your confirmed lab signals here.”

Avoid:
- “History” as a separate module
- fake intelligence
- overexplaining

If Home CTA currently says:
“View Lab History”

It can remain temporarily, but ideally update to route to the Labs history view if implemented.

Example:
- “View Lab History” → `/labs/upload?view=history`

Do NOT overhaul Home copy broadly.

────────────────────
TASK 7 — QA EXPECTATIONS
────────────────────

Validate:

1. Bottom nav shows:
   Home · Labs · Protocol · Profile

2. Labs opens to Snapshot by default.

3. Snapshot view still works exactly as before.

4. History toggle shows archive correctly.

5. Existing History data still renders.

6. Delete session still works.

7. Detail sheets still open.

8. Safety behavior in History remains intact.

9. Mobile spacing works on iPhone SE width.

10. Build passes.

────────────────────
DO NOT CHANGE
────────────────────

Do NOT change:
- onboarding
- auth guards
- Supabase schema
- OCR
- API routes
- decision engine
- safety thresholds
- biomarker classification
- profile
- dashboard insight logic
- Protocol page content
- global styling

────────────────────
RETURN FORMAT
────────────────────

Return:

1. Files inspected
2. Files changed
3. Bottom nav changes
4. Labs toggle implementation
5. How History was integrated
6. Route behavior for `/labs/history`
7. Home CTA behavior if changed
8. What was intentionally not changed
9. Build result
10. Confirmation no files outside `.migration-backup/` were modified