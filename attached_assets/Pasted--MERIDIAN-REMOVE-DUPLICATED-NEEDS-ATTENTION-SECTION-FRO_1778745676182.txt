# MERIDIAN — REMOVE DUPLICATED NEEDS ATTENTION SECTION FROM LABS SNAPSHOT

We need to simplify the Labs Snapshot screen by removing duplicated information.

Important context:
The real app lives in:

.migration-backup/

Vercel production is working because Root Directory is set to .migration-backup.

Do NOT restructure the repo.
Do NOT move files out of .migration-backup.
Do NOT touch root package files.
Do NOT touch Vercel settings.
Do NOT touch artifacts/.
Do NOT publish to Replit.

Modify ONLY:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- profile
- protocol
- onboarding
- OCR routes
- confirm route
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create new files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Remove the duplicated “Needs Attention” marker card section from the Labs Snapshot tab.

CURRENT ISSUE:
Labs Snapshot now has a stronger Current Markers system with:
- Clinical Panels
- Signal Map
- grouped marker rows
- Watch / Attention / Optimal visual states inside each group

Because of this, the old “Needs Attention” section above View Mode is now redundant and makes the page feel too crowded.

GOAL:
Make Labs Snapshot cleaner, calmer, and more premium.

Keep:
- BIOMARKER SIGNALS chip
- Labs title
- subcopy
- Snapshot / History segmented control
- Recent Lab Snapshot section
- summary stats card
- status chips/counts: Optimal / Watch / Attention
- View Mode section
- Clinical Panels / Signal Map toggle
- Current Markers section
- marker rows
- marker detail sheet
- upload flow
- history tab

Remove:
- the full “NEEDS ATTENTION” heading
- the large individual Needs Attention marker cards shown above View Mode
- duplicated eGFR / Chloride / Vitamin D / TSH / etc. cards in that top section

Important:
Do NOT remove the Attention/Watch markers from Current Markers.
They should still appear inside Clinical Panels and Signal Map, sorted by priority.

Behavior after change:
Labs Snapshot should flow like this:

BIOMARKER SIGNALS

Labs
Your clinical markers, translated into biological signals.

[ Snapshot | History ]

Recent Lab Snapshot
Latest collection / Biomarkers / Panels
[ Optimal ] [ Watch ] [ Attention ]

View Mode
[ Clinical Panels ] [ Signal Map ]

Current Markers
[grouped panels/layers with markers]

Do not change state logic.
Do not change range bar logic.
Do not change grouping logic.
Do not change upload review.
Do not change History grouping.

After editing:
1. Report exact file changed.
2. Confirm no unrelated files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload

Confirm:
- Needs Attention section is gone from Snapshot
- status chips/counts still show
- Clinical Panels still works
- Signal Map still works
- Watch/Attention markers still appear inside Current Markers
- marker rows still open detail sheet
- Snapshot / History segmented control still works
- History tab still works
- upload flow still works
- build passes

Do not commit.
Do not push.
Do not publish.