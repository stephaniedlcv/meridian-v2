We need to fix/complete Biomarker Range Bar V1.

Current issue:
The range bar is not visible in places where it should appear.

Examples from /labs/history:
Basophils Pct
Value: 0.4 %
Reference range: 0–2 %
This should show a range bar because it has a numeric value and a numeric reference range.

Product requirement:
Every numeric biomarker with usable range data should show a compact range bar wherever individual biomarker rows are displayed.

The bar should appear in:
1. /labs/history expanded panel biomarker rows
2. /labs/upload Recent Lab Snapshot “Needs Attention” biomarker cards, when range data is available

Do NOT show the bar on high-level panel summary cards, because those are panel-level summaries, not individual biomarker rows.

Modify ONLY these files if needed:

1. .migration-backup/src/app/labs/history/page.tsx
2. .migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- api/insight
- OCR routes
- confirm route
- canonical dictionary
- Supabase clients
- database types
- middleware
- onboarding
- NavBar
- package.json
- artifacts/
- dependencies

Goal:
Display a compact visual biomarker range bar for every numeric biomarker row that has enough range data.

Data fields available:
- value
- unit
- reference_range_min
- reference_range_max
- optimal_range_min
- optimal_range_max
- state

Required behavior:
1. Show the range bar when:
   - value is numeric
   - reference_range_min is numeric
   - reference_range_max is numeric
   - reference_range_min < reference_range_max

2. Do NOT require optimal range for the bar to render.
   If optimal_range_min and optimal_range_max exist, show the optimal segment.
   If they do not exist, still show a simple reference range bar.

3. The user’s result must be marked with a visible circular dot on the bar.

4. Dot position:
   percent = ((value - min) / (max - min)) * 100
   clamp between 0 and 100.

5. If value is below min:
   - dot stays at far left
   - dot uses the biomarker state color

6. If value is above max:
   - dot stays at far right
   - dot uses the biomarker state color

7. Dot colors:
   - Optimal: teal
   - Watch: yellow
   - Attention: orange
   - Critical: red
   - fallback: cyan/teal

8. Use Meridian colors:
   - teal/cyan for normal/optimal
   - yellow for Watch
   - orange for Attention
   - red for Critical
   - dark glass base

9. The bar should be compact and mobile-friendly.
10. The bar must be visible on dark background.
11. It should sit below the marker’s value/reference text.
12. It should not disrupt the existing row layout.
13. Add enough bottom padding so the bottom nav does not cover content.

Labels:
Use subtle labels such as:
- Low
- Optimal
- High

or:
- Ref min
- Optimal
- Ref max

Keep labels small and muted.

Do not show bars for qualitative or non-numeric values:
- Negative
- Trace
- Few
- Moderate
- Many
- Clear
- Yellow
- Cloudy
- Blood type
- empty values
- unit-only rows

Implementation guidance:
Create or reuse local helper functions inside each file only:
- parseNumericValue
- isNumericRange
- getRangePosition
- getStateColor
- BiomarkerRangeBar

If possible, keep the helper code identical in both files for consistency, but do not create a new shared file in this task.

Also add temporary console logs only if necessary to diagnose why the bar is not rendering:
- marker name
- value
- reference_range_min
- reference_range_max
- shouldShowRangeBar

Remove noisy logs if the issue is solved before final report.

After editing:
1. Report exact files changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/history
   /labs/upload
5. Confirm build passes.
6. Confirm that these examples show a bar:
   - Basophils Pct 0.4 % with Ref 0–2 %
   - Hemoglobin 14.3 g/dL with Ref 11–16 g/dL
   - HDL Cholesterol 47.1 mg/dL with reference range
   - eGFR 84 mL/min/1.73 m² with reference range, if range data exists
7. Confirm that qualitative urine values do not show bars.
8. Explain when the range bar appears and when it is intentionally hidden.
9. Do not commit.
10. Do not push.
11. Do not publish to Replit.