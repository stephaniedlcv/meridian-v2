# MERIDIAN — PROFILE FOUNDATION AUDIT ONLY

We need to audit the current Profile and Onboarding foundation before implementing new user context fields.

Important context:
The real app lives in:

.migration-backup/

This is AUDIT ONLY.

Do NOT edit files.
Do NOT modify code.
Do NOT create files.
Do NOT add dependencies.
Do NOT commit.
Do NOT push.
Do NOT publish.

Goal:
Determine what Meridian currently collects, stores, displays, and allows the user to edit in Profile/Onboarding.

We are preparing the Profile Intelligence Foundation so Meridian can eventually give clearer, more personalized, less ambiguous recommendations.

Do not implement anything yet.

Relevant product direction:
Meridian should collect date_of_birth, not age.
Age should be calculated dynamically over time.

Meridian should support stable identity fields and dynamic context fields.

Stable identity:
- first_name
- last_name
- display_name
- email
- date_of_birth
- biological_profile
- language_preference

Dynamic context:
- height
- current_weight
- activity_level
- training_days_per_week
- primary_goal
- body_goal_phase
- diet_pattern
- medications
- supplements
- wearables
- medical_context

Important:
Many dynamic fields must be editable later because they change over time:
- weight
- activity level
- body goal phase
- diet pattern
- medications
- supplements
- wearables

Do not add these yet. Audit current support first.

FILES / AREAS TO INSPECT:

1. Profile page
Look for:
- .migration-backup/src/app/profile/page.tsx
- any related profile components

2. Onboarding pages
Look for:
- .migration-backup/src/app/onboarding/welcome/page.tsx
- .migration-backup/src/app/onboarding/profile/page.tsx
- .migration-backup/src/app/onboarding/connect/page.tsx
- any other onboarding routes

3. API routes
Look for:
- profile API routes
- onboarding API routes
- auth routes that create/update profiles

4. Types
Look for:
- .migration-backup/src/types/database.ts
- any profile types/interfaces

5. Supabase usage
Look for:
- profile table field references
- profile insert/update/upsert logic
- onboarding_completed logic

6. Any schema/migration files if present
Look for:
- SQL files
- migration files
- table definitions

AUDIT CHECKLIST:

1. Current profile fields

Report exactly which profile fields currently exist in:
- database types
- API logic
- Profile UI
- Onboarding UI

For each field, report:
- exists in DB/types?
- collected in onboarding?
- displayed in profile?
- editable in profile?
- required or optional?
- stored format/type?

2. Current onboarding flow

Report current onboarding steps and what each step collects.

Include:
- welcome
- profile
- connect
- any other step

Confirm:
- does onboarding collect full name?
- does onboarding collect first name and last name separately?
- does onboarding collect date_of_birth?
- does onboarding collect biological_profile?
- does onboarding collect user_profile / goal?
- does onboarding collect medications?
- does onboarding collect height/weight/activity?
- does onboarding collect language preference?
- does onboarding collect wearable preferences?

3. Current Profile page behavior

Audit the current profile page.

Report:
- what data it displays
- what data it lets the user edit
- whether image/avatar upload exists
- whether it saves changes
- what API or Supabase call it uses
- whether missing fields show placeholders
- whether any hardcoded demo values exist

4. Data model gaps

Compare current implementation against the desired Profile Intelligence Foundation.

Create a gap table:

Field | Current support | Needed change | Requires DB migration? | Requires UI change? | Priority

Fields to include:
- first_name
- last_name
- display_name
- email
- date_of_birth
- biological_profile
- language_preference
- height
- current_weight
- activity_level
- training_days_per_week
- primary_goal
- body_goal_phase
- diet_pattern
- medications with name + dose
- supplements
- wearables
- Other/manual wearable
- medical_context
- consent_for_estimated_ranges

5. Schema/migration impact

Determine whether the current database schema can support the desired fields.

Report:
- fields already present
- fields missing
- fields present but not ideal
  Example: full_name exists but first_name/last_name are preferred.
- fields currently jsonb
- fields that should be jsonb
- fields that should be text/enums
- fields that need updated_at

Do not write SQL yet.
Only recommend schema changes.

6. Medication field audit

Report current medication support.

Check:
- is medication collected?
- is it a text field, array, or jsonb?
- can user enter dose?
- can user enter frequency?
- can user edit medications after onboarding?

Desired future structure:
[
  {
    "name": "Tirzepatide",
    "dose": "5 mg",
    "frequency": "weekly",
    "start_date": "2025-11-14",
    "notes": ""
  }
]

Do not implement autocomplete now.
Just note that typeahead/autocomplete is future phase.

7. Wearables audit

Report current wearable support.

Check:
- which wearables are listed
- whether user can select multiple
- whether user can choose Other / manual entry
- whether wearable preferences are saved
- whether unsupported wearable copy exists

Desired future behavior:
If user selects Other / Not listed, allow manual entry and show a message:
“Meridian is working to stay current with new devices. We’ll save this wearable preference and review whether support can be added in the future.”

Do not implement yet.

8. Language preference audit

Report whether Meridian currently supports:
- language_preference field
- English / Español choice
- translating UI copy
- keeping biomarker names in English while localizing explanations

Do not implement language system yet.
Just audit current readiness.

9. Consent for estimated ranges

Audit whether there is any existing consent/opt-in for Meridian estimating:
- protein range
- hydration range
- activity range
- educational targets

Desired future copy:
“Meridian uses evidence-informed health references and your profile data to estimate educational ranges. These are not prescriptions and should be adjusted with a qualified professional when needed.”

Report:
- current support
- missing pieces
- where it should live later: onboarding, profile, or both

10. Risk audit

Identify risks if we implement all fields too aggressively now.

Examples:
- onboarding becomes too long
- schema migration could break current profile
- too many required fields could block users
- medication/dose field needs careful UX
- language system should be separate
- estimated nutrition ranges require careful disclaimer

11. Recommended implementation phases

Recommend a phased plan:

Phase A:
Minimum profile fields needed now.

Phase B:
Editable dynamic context.

Phase C:
Language preference.

Phase D:
Medication/supplement structured editor.

Phase E:
Wearables Other/manual support.

Phase F:
Estimated range consent + protein/hydration/activity guidance.

Do not implement.
Just recommend.

BUILD:

Run:

cd .migration-backup && npm run build

Report:
- build result
- TypeScript errors if any
- unrelated warnings if any

OUTPUT REQUIRED:

Return a structured audit with:

1. Executive summary
2. Current profile/onboarding fields
3. Current Profile page behavior
4. Current onboarding flow
5. Current schema/type support
6. Gap table
7. Medication support findings
8. Wearable support findings
9. Language support findings
10. Consent/estimated ranges findings
11. Risks
12. Recommended phased implementation plan
13. Exact files inspected
14. Build result
15. Confirmation:
   “No files were changed.”

Remember:
Do not edit anything.
Audit only.