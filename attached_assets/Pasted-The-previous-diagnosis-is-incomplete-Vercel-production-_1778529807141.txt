The previous diagnosis is incomplete. Vercel production DOES show recent deploys, including the dashboard lab-data fallback commit, so do not assume the code is undeployed.

Current issue:
Production /dashboard still shows “Upload your first lab PDF” even though /labs/history shows saved biomarkers for the same logged-in user.

Goal:
Make dashboard robustly distinguish true no-data from saved-labs state.

Modify ONLY:

1. .migration-backup/src/app/dashboard/page.tsx
2. .migration-backup/src/app/api/insight/route.ts

Do not touch:
- /labs/history
- /labs/upload
- OCR routes
- confirm route
- Supabase clients
- database types
- middleware
- onboarding
- NavBar
- package.json
- artifacts/
- dependencies

Required fix 1 — dashboard/page.tsx:
Do not depend only on /api/insight returning state === 'no_data' to check saved biomarkers.

Instead:
- After auth.getUser(), perform a lightweight biomarkers_static count for the logged-in user.
- Store whether the user has any saved biomarkers.
- Then call /api/insight.
- If /api/insight returns a valid solved/calibrating state, render that normally.
- If /api/insight returns no_data, success false, or throws an error:
  - if biomarker count > 0, set state to labs_saved
  - if biomarker count = 0, set state to no_data

This means:
- true no_data only happens when biomarkers_static count is 0.
- labs_saved happens when biomarkers exist but insight cannot produce a current insight.

Add temporary console logs only if useful:
- user.id
- biomarker count
- insight response state
- final dashboard state

Required fix 2 — insight/route.ts:
Remove the hard 1-year collected_at filter:

.gte('collected_at', oneYearAgo.toISOString())

Reason:
Meridian must keep and consider historical labs. The decision engine can decide how much weight to give older data, but the API should not make the dashboard think valid saved labs do not exist.

Keep:
- user_id filtering
- collected_at ordering
- latestByMarker logic
- existing decision engine logic

Rules:
- Minimal logic fix only.
- Do not redesign.
- Do not change copy unless needed.
- Do not add dependencies.
- Do not create files.
- Do not commit.
- Do not push.
- Do not publish to Replit.

After editing:
1. Report exact files changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /dashboard
   /api/insight if possible
5. Confirm build passes.
6. Explain why the dashboard will now show Labs received whenever the user has any saved biomarkers.