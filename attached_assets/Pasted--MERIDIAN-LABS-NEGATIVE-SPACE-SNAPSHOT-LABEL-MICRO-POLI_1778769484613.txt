# MERIDIAN — LABS NEGATIVE SPACE + SNAPSHOT LABEL MICRO-POLISH

We need a very small visual polish on the Labs page.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- profile
- protocol
- onboarding
- OCR routes
- confirm route
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create new files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Polish Labs spacing and reduce visual heaviness in the snapshot/control area.

This is visual polish only.

Do NOT change:
- upload flow
- Snapshot / Timeline logic
- Clinical Panels / Signal Map logic
- marker grouping
- marker sorting
- state badges
- status counts
- range bars
- Timeline data
- marker detail sheet

REQUIREMENTS:

1. Simplify snapshot label

Change:

CURRENT BIOMARKER SNAPSHOT · [currentYear]

to:

BIOMARKER SNAPSHOT · [currentYear]

Do not hardcode the year.
Keep using the existing dynamic currentYear value.

Reason:
“Current” is implied by Snapshot, but “Biomarker” keeps the label clinically clear.

2. Increase spacing before VIEW MODE

The spacing between the status chips row:

Optimal / Watch / Attention

and the VIEW MODE section feels too tight.

Increase the top margin above VIEW MODE so it reads as a new secondary interaction section.

Use a clean, mobile-friendly spacing value around 32px.

Do not create excessive empty space.

3. Keep VIEW MODE label style

Keep VIEW MODE as:
- uppercase
- small
- letter-spaced
- muted teal / secondary
- no colon
- not white/bold

4. Refine View Mode microcopy

Keep the dynamic microcopy:

Clinical Panels selected:
Grouped by clinical lab panels.

Signal Map selected:
Grouped by biological signal layers.

But make sure it feels like a caption attached to the segmented control:
- small
- muted
- close enough to the control to feel connected
- not visually competing with CURRENT MARKERS

5. Preserve structure

The area should read:

BIOMARKER SNAPSHOT · 2026

[ Snapshot | Timeline ]

[summary card]

[status chips]


VIEW MODE

[ Clinical Panels | Signal Map ]

Grouped by clinical lab panels.


CURRENT MARKERS

6. Build and test

After editing:
1. Report exact file changed.
2. Confirm no unrelated files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload

Confirm:
- Snapshot label now reads BIOMARKER SNAPSHOT · [currentYear]
- Year remains dynamic
- Spacing between status chips and VIEW MODE feels more intentional
- View Mode microcopy feels attached to the segmented control
- Snapshot still works
- Timeline still works
- Clinical Panels still works
- Signal Map still works
- upload flow still works
- build passes

Do not commit.
Do not push.
Do not publish.