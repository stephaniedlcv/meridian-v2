# MERIDIAN — FIX /api/insight 500
# Phase 1 stability task

You are working on Meridian — a premium Health Intelligence System.

Stack:
- Next.js 14 App Router
- TypeScript
- Supabase
- Vercel
- Anthropic API
- Inline styles only

IMPORTANT:
- Real app lives inside `.migration-backup/`
- Do NOT move files outside `.migration-backup/`
- Do NOT redesign UI
- Do NOT add dependencies
- Do NOT touch artifacts/
- Do NOT touch onboarding/auth
- Do NOT touch Labs UI
- Do NOT touch Profile UI
- Do NOT change Supabase schema unless you find a confirmed missing column required by existing code
- This is a diagnostic + targeted fix task only

PROBLEM:
The browser console shows:

GET /api/insight?user_id=<uuid> 500 Internal Server Error

Dashboard falls back to:
insight unavailable — final state: labs_saved

The app visually survives, but Phase 1 should not ship with the core insight endpoint failing.

GOAL:
Find the exact cause of `/api/insight` 500 and fix it with the smallest safe change.

────────────────────
TASK 1 — INSPECT /api/insight
────────────────────

Inspect:

- `.migration-backup/src/app/api/insight/route.ts`
- any files imported by that route:
  - decision engine
  - health intelligence utilities
  - Anthropic client usage
  - Supabase server/service role client
  - database types used by the route

Determine exactly where the 500 is thrown.

Do NOT guess.

────────────────────
TASK 2 — ADD TEMPORARY SERVER-SIDE ERROR LOGGING IF NEEDED
────────────────────

If the cause is not obvious from code, add safe server-side logging only.

Allowed logs:
- error code
- error message
- failing step label
- whether required env vars exist as booleans

Do NOT log:
- Anthropic API key
- Supabase service role key
- full lab values if unnecessary
- sensitive user data

────────────────────
TASK 3 — CHECK COMMON FAILURE POINTS
────────────────────

Specifically verify:

1. Environment variables:
   - ANTHROPIC_API_KEY exists
   - SUPABASE_SERVICE_ROLE_KEY exists
   - NEXT_PUBLIC_SUPABASE_URL exists

2. Supabase query:
   - route uses correct Supabase URL
   - route uses service role only server-side
   - queried columns exist in current schema
   - query does not require missing tables/columns

3. Data shape:
   - route handles users with labs but no complete insight context
   - route handles 1 biomarker
   - route handles missing wearable data
   - route handles missing feedback data
   - route handles missing trend history

4. Anthropic:
   - route handles Anthropic failure gracefully
   - no uncaught throw should produce generic 500 if fallback insight is possible

5. JSON response:
   - route always returns valid JSON
   - dashboard can consume response shape safely

────────────────────
TASK 4 — TARGETED FIX
────────────────────

Fix only the confirmed cause.

Acceptable fixes:
- correct bad Supabase select column
- handle empty/null data
- add missing guard
- fix Anthropic request shape
- return fallback JSON instead of throwing
- correct env var usage
- update type mismatch

Not acceptable:
- rewrite decision engine
- create fake intelligence
- remove safety handling
- hide all errors without logging
- redesign dashboard
- hardcode user-specific data

────────────────────
TASK 5 — FALLBACK BEHAVIOR
────────────────────

If insight generation fails because:
- Anthropic unavailable
- insufficient data
- missing wearable data
- no trend history

The route should return a calm valid fallback response, not 500.

Suggested response shape should match what dashboard already expects.

The dashboard should continue showing a truthful state like:
- labs_saved
- baseline building
- insight unavailable
without breaking the console.

Do NOT pretend a real insight exists if it was not generated.

────────────────────
ACCEPTANCE CRITERIA
────────────────────

After fix:

1. `/api/insight?user_id=<valid uuid>` does not return 500 for a user with saved labs.
2. Route returns valid JSON.
3. Dashboard no longer shows red 500 error in console.
4. If no generated insight is available, route returns a truthful fallback.
5. No fake medical advice.
6. Build passes.
7. No UI redesign.
8. No files outside `.migration-backup/` modified.

────────────────────
RETURN FORMAT
────────────────────

Return:

1. Files inspected
2. Files changed
3. Exact root cause of 500
4. Exact fix made
5. Fallback behavior added or preserved
6. Any logs added or removed
7. Build result
8. Confirmation no files outside `.migration-backup/` were modified