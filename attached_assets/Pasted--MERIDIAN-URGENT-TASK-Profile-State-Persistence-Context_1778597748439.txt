# MERIDIAN — URGENT TASK: Profile State Persistence + Context Truth Pass

You are working on Meridian — a premium Health Intelligence System.

Stack:
- Next.js 14 App Router
- TypeScript
- Supabase
- Vercel
- Inline styles only

IMPORTANT:
- The real app lives inside `.migration-backup/`
- Do NOT move files
- Do NOT restructure repo
- Do NOT redesign UI
- Do NOT add dependencies
- Do NOT touch artifacts/
- Do NOT change routes unless absolutely necessary
- Do NOT implement new Phase 2 features
- This is a Phase 1 state persistence + truthfulness fix

PROBLEM:
Profile appears to update locally, but some values are lost after navigating away or refreshing.

Observed issues:
1. Display name appears after saving, but disappears after changing pages.
2. Health Focus Mode does not persist.
3. Age context shows Pending even though the user entered Date of Birth during onboarding.
4. Medication context shows Pending, but there is no clear way for the user to add/update medications later.
5. UI status chips may be showing “pending” even when the underlying profile data exists.

GOAL:
Make Profile reflect the real persisted Supabase profile state truthfully.

The UI must not claim:
- Not set
- Pending
- Focus not set

if the corresponding data exists in Supabase.

────────────────────
CRITICAL RULES
────────────────────

- Use `profiles.full_name` for display name.
- Do NOT use `preferred_name`; that field does not exist yet.
- Do NOT use email prefix as a fallback display name.
- If `profiles.full_name` is null/empty, show “Not set yet”.
- If `profiles.full_name` exists, show it everywhere Profile needs display name.

- Use `profiles.birth_date` for age context.
- If `birth_date` exists, Age context should be Active.
- If `birth_date` is missing, Age context can be Pending.

- Use `profiles.medications` for medication context.
- If medications exists and contains usable content, Medication context should be Active.
- If medications is null/empty, show Pending or Not added yet, but include a clear CTA/future-ready explanation.

- Use `profiles.user_profile` for Health Focus Mode.
- If user_profile exists, Focus Mode should show the correct selected mode.
- If user_profile is missing, show Focus not set.

────────────────────
TASK 1 — Investigate Persistence Root Cause
────────────────────

Inspect:

- onboarding identity save logic
- onboarding goals save logic
- profile fetch logic
- profile update/edit logic
- Supabase profile update calls
- local state hydration
- any profile cache/state overwrite behavior

Look for mismatches such as:
- `display_name` vs `full_name`
- `preferred_name` vs `full_name`
- `health_focus` vs `user_profile`
- enum mismatch
- optimistic local state not persisted
- reload/fetch overwriting saved state
- missing select fields in Supabase query

────────────────────
TASK 2 — Fix Display Name Persistence
────────────────────

Expected behavior:

1. User enters display name in onboarding identity.
2. It saves to `profiles.full_name`.
3. Profile page reads `profiles.full_name`.
4. Name remains after:
   - page navigation
   - refresh
   - logout/login

If editing profile name exists:
- updating name should write to `profiles.full_name`
- UI should refresh from persisted value

Do NOT create new database fields.

────────────────────
TASK 3 — Fix Health Focus Mode Persistence
────────────────────

Expected behavior:

1. User selects Meridian mode during onboarding goals.
2. It saves to `profiles.user_profile`.
3. Profile shows selected Health Focus Mode.
4. Personalization Matrix should show Focus context as Active if user_profile exists.
5. It persists after navigation/refresh/logout-login.

If Profile has an “Adjust” or “Set focus” CTA:
- it should not fake-save locally only
- either persist to `profiles.user_profile`
- or clearly show coming-soon behavior if editing is not implemented yet

────────────────────
TASK 4 — Fix Age Context Truth
────────────────────

Expected behavior:

If `profiles.birth_date` exists:
- Age context = Active
- do not show Pending

If possible, display a subtle contextual line:
“Used for age-adjusted biological context.”

Do NOT implement complex age-based algorithms here.

Do NOT expose exact age if current UI does not require it.

────────────────────
TASK 5 — Fix Medication Context Truth + CTA
────────────────────

Expected behavior:

If `profiles.medications` has content:
- Medication context = Active

If medications is empty/null:
- Medication context = Pending or Not added yet
- provide clear explanation:
  “Add medications to help Meridian interpret labs more safely.”

If medication editing is not implemented:
- CTA may say:
  “Add medications”
- but should either:
  A) open a simple existing edit flow if available, OR
  B) show a calm coming-soon/future-ready state

Do NOT implement a full medication interaction engine.

Do NOT provide medical advice.

────────────────────
TASK 6 — Status Chips Must Reflect Data Truth
────────────────────

Profile header chips should reflect persisted data:

- Profile active: true if profile row exists
- Biological profile: active if biological_profile exists
- Focus: active if user_profile exists, otherwise Focus not set
- Age context: active if birth_date exists
- Medication context: active if medications exists

Avoid showing “pending” when data exists.

────────────────────
TASK 7 — No Fake Fallbacks
────────────────────

Do NOT use:
- email prefix as display name
- fake sample name
- hardcoded Stephanie
- local-only state as source of truth after save

Supabase profile row should be the source of truth.

────────────────────
ACCEPTANCE CRITERIA
────────────────────

After implementation:

1. Display name persists across page navigation.
2. Display name persists after browser refresh.
3. Display name persists after logout/login.
4. Health Focus Mode persists.
5. Age context shows Active when birth_date exists.
6. Medication context shows Active when medications exists.
7. Medication context has clear CTA/explanation when empty.
8. Profile chips/statuses match real Supabase data.
9. No fake display name fallback is used.
10. Build passes.
11. No files outside `.migration-backup/` are modified.

────────────────────
RETURN FORMAT
────────────────────

Return:

1. Files inspected
2. Files changed
3. Root cause found
4. Display name persistence fix
5. Focus mode persistence fix
6. Age context truth fix
7. Medication context behavior fix
8. Status chip truth updates
9. What was intentionally not implemented
10. Build result
11. Confirmation no files outside `.migration-backup/` were modified