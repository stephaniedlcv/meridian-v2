# MERIDIAN — LABS CURRENT MARKERS RANGE DISPLAY FIX

We need to fix the range display inside the new Labs Snapshot Current Markers view.

Important context:
The real app lives in:

.migration-backup/

Vercel production is working because Root Directory is set to .migration-backup.

Do NOT restructure the repo.
Do NOT move files out of .migration-backup.
Do NOT touch root package files.
Do NOT touch Vercel settings.
Do NOT touch artifacts/.
Do NOT publish to Replit.

Modify ONLY:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- profile
- protocol
- onboarding
- labs/history
- OCR routes
- confirm route
- canonical dictionary
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies

TASK:
Fix how reference ranges and optimal ranges are displayed in the new Labs Snapshot Current Markers section.

Current issue:
The new Clinical Panels / Signal Map structure is correct, but some marker rows are showing confusing or incorrect range labels.

Examples observed:
- Triglycerides 66.4 mg/dL shows Optimal, but the row displays “Ref 150–199”
- Total Cholesterol 155 mg/dL shows Optimal, but displays “Ref 200–239”
- LDL Cholesterol 95 mg/dL shows Optimal, but displays “Ref 130–159”
- HDL Cholesterol shows “Range data not available” even though it should have some valid range/optimal context if available in the biomarker data.

Problem:
The UI appears to be displaying category/borderline/high ranges or incorrect range fields as “Ref”.
This is misleading because the marker can be Optimal while the displayed “Ref” looks like a high/borderline range.

GOAL:
Make range display accurate, conservative, and clinically safe.

Do not invent ranges.
Do not create a Reference Range Engine.
Do not add new fallback clinical ranges.
Use only the range fields already available on the biomarker data.

REQUIREMENTS:

1. Preserve the new Labs UX structure

Do NOT remove:
- Clinical Panels / Signal Map toggle
- Current Markers section
- Needs Attention
- Recent Lab Snapshot
- Snapshot / History segmented control
- marker detail sheet behavior

This is a range display fix only.

2. Inspect current range display logic

Find the helper or inline logic that renders:
- Ref pills
- Optimal pills
- Range bars
- “Range data not available.”

Verify which fields are being used:
- reference_range_min
- reference_range_max
- optimal_range_min
- optimal_range_max
- value
- state

3. Ref pill rules

Only show a “Ref min–max” pill when BOTH:
- reference_range_min is a finite number
- reference_range_max is a finite number
- reference_range_min < reference_range_max

Do not show “Ref” using:
- category ranges
- high/borderline ranges
- state threshold bands
- any derived range that is not explicitly the clinical reference range

If the current logic is using state thresholds or internal category bounds as Ref, stop doing that.

4. Optimal pill rules

Only show an “Optimal min–max” pill when BOTH:
- optimal_range_min is a finite number
- optimal_range_max is a finite number
- optimal_range_min < optimal_range_max

If both clinical reference and optimal range exist:
- Prefer showing “Optimal min–max” for markers whose state is Optimal
- Show “Ref min–max” secondarily only if it fits cleanly
- Do not overcrowd the row

If only clinical reference exists:
- show “Ref min–max”

If only optimal range exists:
- show “Optimal min–max”

If neither exists:
- show subtle text:
  Range data not available.

5. Range bar rules

Only render a range bar when:
- value is finite
- reference_range_min is finite
- reference_range_max is finite
- reference_range_min < reference_range_max

Do not render blank/ghost bars.

Do not use optimal_range_min/max as the full bar bounds unless the current existing visual component already clearly labels it as Optimal-only.
Prefer no bar over a misleading bar.

6. HDL issue

Investigate why HDL Cholesterol shows “Range data not available.”

If HDL has valid reference_range_min/max or optimal_range_min/max in the current biomarker object, make sure the Current Markers row uses them.

If HDL truly lacks range fields in the data object:
- keep “Range data not available.”
- do not invent fallback ranges in this task.

7. Cholesterol category issue

For lipid markers like:
- Triglycerides
- Total Cholesterol
- LDL Cholesterol
- HDL Cholesterol

Do not display high/borderline threshold bands as “Ref”.

Example:
If Triglycerides is 66.4 and state is Optimal, do not display “Ref 150–199” because that reads like the current reference range and conflicts with the Optimal state.

If the available range field appears inverted or semantically suspicious for an Optimal marker, prefer hiding the Ref pill and showing:
Range data not available.

Be conservative.

8. Marker detail sheet

Do not break the existing biomarker detail sheet.

If the detail sheet has its own range display, do not modify it unless it uses the same broken helper and must be fixed for consistency.

9. Visual style

Keep UI clean:
- no extra long explanations
- no medical advice
- no diagnosis
- no large warning copy
- subtle range labels only
- preserve Meridian dark/glass style

10. Build and test

After editing:
1. Report exact file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload

Confirm:
- Clinical Panels still works
- Signal Map still works
- Current Markers still renders
- Triglycerides no longer shows misleading “Ref 150–199” while Optimal
- Total Cholesterol no longer shows misleading “Ref 200–239” while Optimal
- LDL Cholesterol no longer shows misleading “Ref 130–159” while Optimal
- HDL uses valid range fields if present, otherwise shows “Range data not available”
- range bars only show when clinical reference min/max are valid
- no fake/blank range bars
- marker rows still open detail sheet
- upload flow still works
- build passes

Do not commit.
Do not push.
Do not publish.