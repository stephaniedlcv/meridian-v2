Approved. Implement the smallest safe labs persistence/display fix.

Modify ONLY these files:

1. .migration-backup/src/app/api/insight/route.ts
2. .migration-backup/src/app/dashboard/page.tsx
3. .migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- API OCR extraction route
- API OCR confirm route
- Supabase client files
- database types
- middleware
- onboarding files
- artifacts/
- package.json
- visual redesigns
- dependencies

Required fixes:

1. insight/route.ts
Fix the profile query so the insight pipeline does not fail if the profiles table does not have a safety_status column.

Current suspected issue:
The route selects:
biological_profile, user_profile, medications, safety_status

If safety_status does not exist, the query fails and the entire insight pipeline returns success: false, making the dashboard think there is no data.

Smallest safe fix:
- Remove safety_status from the profiles select.
- If the code references safety_status later, provide a safe fallback value in code, such as null or 'normal', without requiring the database column.
- Do not change biomarker query logic.
- Do not change the decision engine unless absolutely necessary.

2. dashboard/page.tsx
Do not silently convert all insight API failures into no_data.

Current issue:
If /api/insight returns success: false or throws, dashboard sets state to no_data, which makes saved biomarkers appear invisible.

Required:
- If the API response succeeds and returns a real no-data state, keep no_data behavior.
- If the API response fails because of an error, show or preserve a soft error/loading-safe state instead of pretending there are no labs.
- At minimum, log the error clearly and set a distinct state if the current component supports it.
- Keep UI changes minimal.

3. labs/upload/page.tsx
After successful confirm/save:
- Keep the success state visible briefly.
- After about 2 seconds, automatically route to /dashboard.
- Use router.push('/dashboard').
- Do not change extraction logic.
- Do not change confirm/save payload.
- Do not change Supabase insert logic.

Rules:
- Minimal logic fix only.
- No redesign.
- Do not add image/photo upload yet.
- Do not implement history yet.
- Do not add dependencies.
- Do not create files.
- Do not commit.
- Do not push.
- Do not publish to Replit.

After editing:
1. Report every file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   - /dashboard
   - /labs/upload
   - /api/insight if possible
5. Confirm build passes.
6. Explain exactly why the dashboard should now see saved biomarkers after upload.