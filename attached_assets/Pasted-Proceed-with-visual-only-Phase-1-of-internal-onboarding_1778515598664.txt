Proceed with visual-only Phase 1 of internal onboarding polish.

Modify ONLY this file:

.migration-backup/src/app/onboarding/goals/page.tsx

Do not touch:
- /onboarding/profile
- /onboarding/connect
- API routes
- Supabase clients
- database types
- middleware
- artifacts/
- package.json
- layout files

Goal:
Make /onboarding/goals visually consistent with the upgraded Meridian landing and welcome/auth screens.

Required visual-only changes:
1. Fix the Fraunces font reference to use:
   "var(--font-fraunces), serif"
   where applicable.

2. Reduce the main content maxWidth to feel mobile-first and aligned with the welcome/auth screen.
   Target: around 480px.

3. Update ambient background:
   - use 3 soft orbs instead of 2
   - include a subtle center orb
   - reduce opacity to match landing/welcome
   - blur around 90px
   - keep the dark biological OS feel

4. Restyle the “Extended profile” label as a small premium pill/chip:
   - uppercase
   - small glowing dot if appropriate
   - subtle border
   - teal/cyan styling
   - consistent with the “BIOLOGICAL INTELLIGENCE SYSTEM” / signal chip language

5. Update the main glass card:
   - remove heavy dark drop shadow
   - use subtle glassmorphism
   - add inner top-edge highlight
   - add faint teal/cyan bloom
   - borderRadius around 24px

6. Update error message color to red:
   #EF4444

7. Update CTA button:
   - borderRadius: 14px
   - fontWeight: 700
   - teal/cyan glow
   - consistent with welcome/auth CTA button

8. Preserve all current content, fields, form behavior, and routing.

Critical rules:
- Visual-only edits.
- Do not change Supabase logic.
- Do not change auth guard behavior.
- Do not change handleContinue behavior.
- Do not change router.push behavior.
- Do not change field names or form state.
- Do not add dependencies.
- Do not create files.
- Inline styles only.
- Keep the app mobile-first, premium, futuristic, calm, clinical, and readable.

After editing:
1. Report the exact file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /onboarding/goals
5. Confirm build passes.
6. Do not commit.
7. Do not push.
8. Do not publish to Replit.