# MERIDIAN — HOME REMOVE UPLOAD LABS CTA AFTER LABS EXIST

We need a small Home/Dashboard UX logic polish.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:

.migration-backup/src/app/dashboard/page.tsx

Do not touch:
- labs page
- upload flow
- OCR routes
- confirm route
- profile
- protocol
- onboarding
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create new files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Remove the large “Upload Labs” CTA card from Home when the user already has saved biomarkers/labs.

CURRENT ISSUE:
Home currently shows an “Upload Labs” card even when the user already has labs saved and Meridian is already showing an active insight.

This is redundant because the user can upload additional labs from the Labs tab.

GOAL:
Home should stay focused on today’s biological signal and action steps.
Labs should remain the dedicated place to upload, review, and manage lab PDFs.

PRODUCT RULE:
Do not duplicate core workflows across multiple pages unless there is a strong onboarding reason.

Home = daily insight and action.
Labs = upload, review, manage, and explore lab data.

REQUIREMENTS:

1. Hide Upload Labs CTA when labs exist

If the user already has saved biomarkers/labs:

hasBiomarkers === true

Do NOT render the large Upload Labs card on Home.

2. Keep Upload Labs CTA only when no labs exist

If the user has no saved biomarkers/labs:

hasBiomarkers === false

Keep the Upload Labs CTA visible because the user still needs to upload labs to unlock Meridian’s intelligence.

3. Do not replace it with another upload link

Do NOT add:
- Add updated labs
- Upload more labs
- Add labs
- any small upload link
- any duplicate upload CTA

When labs exist, Home should not show a lab upload CTA.

The user should use the Labs tab for additional uploads.

4. Do not change upload functionality

Do not change any Labs page upload behavior.
The Labs tab should continue to allow uploading PDFs.

5. Do not change insight logic

Do not change:
- intelligence state
- solved/no_data/labs_saved logic
- safety alert logic
- biomarker count logic
- action steps
- confidence trace
- disclaimer
- bottom navigation
- Labs tab behavior

6. Build and test

After editing:
1. Report exact file changed.
2. Confirm no unrelated files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /dashboard

Confirm:
- When hasBiomarkers === true, the large Upload Labs card is hidden
- No replacement upload link appears on Home
- Active insight still renders normally
- Action steps still render normally
- Confidence trace still renders normally
- Disclaimer still renders normally
- Bottom nav still works
- Labs tab still allows uploading labs
- When hasBiomarkers === false, Upload Labs CTA still appears
- build passes

Do not commit.
Do not push.
Do not publish.