Stop auth work for now.

We found a more critical labs persistence/display issue.

Current production behavior:
1. User uploads a lab PDF.
2. Meridian extracts biomarkers and shows a confirmation state, e.g. “15 biomarkers saved” or “18 markers found”.
3. After confirming, the app still returns to /labs/upload.
4. Dashboard still shows the no-data state asking the user to upload labs.
5. The uploaded/saved biomarkers are not visible in the app.
6. There is also no option to upload labs by taking/uploading a photo image, only PDF.

Do not edit code yet.

Diagnose the full labs flow only.

Inspect these areas:
- /labs/upload page/component
- /labs/history page/component
- /dashboard page
- any lab upload API route
- any biomarker extraction API route
- any Supabase insert/select logic for biomarkers
- database table names used for saved biomarkers
- whether saved biomarkers are tied to user_id
- whether dashboard reads from the same table where upload saves
- whether confirm/save actually inserts rows
- whether collection date / lab encounter is required and missing
- whether RLS could block insert/select
- whether the UI shows success before database save finishes
- whether History route exists and reads saved labs

Diagnosis required:
1. Identify the exact file(s) responsible for upload, confirm/save, dashboard no-data state, and lab history.
2. Identify what table(s) the upload flow writes to.
3. Identify what table(s) the dashboard reads from.
4. Confirm whether those are the same tables.
5. Confirm whether saved biomarkers include user_id.
6. Confirm whether insert errors are being handled or silently ignored.
7. Confirm whether dashboard/history query errors are being handled or silently ignored.
8. Explain why the dashboard still asks for labs after markers are supposedly saved.
9. Explain why the app returns to /labs/upload instead of showing saved results/history/dashboard.
10. Propose the smallest safe fix.
11. Separately propose how to add image/photo upload support for lab screenshots/photos:
    - allowed file types: PDF, JPG, JPEG, PNG, HEIC if feasible
    - do not implement yet
    - identify whether current extraction API can handle images or only PDFs
    - identify exact files that would need changes

Rules:
- Do not modify files yet.
- Do not commit.
- Do not push.
- Do not publish to Replit.
- Report only.