# MERIDIAN — LABS HEADER + SNAPSHOT HIERARCHY POLISH

We need to polish the Labs page hierarchy so the header, Snapshot/Timeline controls, and View Mode feel cleaner, more premium, and more consistent with Meridian’s visual system.

Important context:
The real app lives in:

.migration-backup/

Vercel production is working because Root Directory is set to .migration-backup.

Modify ONLY:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- profile
- protocol
- onboarding
- OCR routes
- confirm route
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create new files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Refine the Labs page header, Snapshot/Timeline placement, and View Mode layout.

This is visual hierarchy/copy polish only.

Do NOT change:
- upload flow
- OCR/extract logic
- confirm/save logic
- delete logic
- Clinical Panels / Signal Map grouping logic
- range bar logic
- biomarker state logic
- Timeline/History data grouping logic
- marker detail sheet behavior

GOAL:
Separate page identity from data controls.

Header should identify the page.
Snapshot/Timeline should live with the snapshot/timeline section.
View Mode should be compact and contextual.

REQUIREMENTS:

1. Keep Labs page identity header

Top header should remain:

BIOMARKER SIGNALS

Labs

Subcopy should use the two-line editorial rhythm, consistent with Home:

Line 1:
Your clinical markers.

Line 2:
Translated into biological signals.

Visual style:
- Line 1 should be stronger / white / semi-bold or bold, matching Home’s primary subcopy style.
- Line 2 should be muted teal/secondary, matching Home’s secondary subcopy style.
- Keep spacing clean and mobile-friendly.

Do not collapse this into one sentence.

2. Move Snapshot / Timeline control

The Snapshot / Timeline segmented control should no longer sit directly under the main Labs header.

Move it into the snapshot section, under the snapshot section title.

Current top area should NOT be:

Labs
subcopy
[ Snapshot | Timeline ]

Instead it should be:

Labs
subcopy

Current Biomarker Snapshot
[ Snapshot | Timeline ]
Current Year · [current year]

3. Rename snapshot section

Change:

Recent Lab Snapshot

to:

Current Biomarker Snapshot

Reason:
“Current” feels like active state.
“Biomarker” avoids repeating “Labs.”

4. Replace Last 12 months label

Replace:

Last 12 months

with:

Current Year · [current year]

Do not hardcode 2026.

Compute the current year dynamically from the current date in JavaScript.

Example:
Current Year · 2026

5. Desired Snapshot hierarchy

The Snapshot area should flow like this:

Current Biomarker Snapshot

[ Snapshot | Timeline ]

Current Year · 2026

Latest collection / Biomarkers / Panels summary

Status chips:
Optimal / Watch / Attention / Critical if present

Then:

Current Markers

6. Timeline hierarchy

When Timeline is selected, keep the same saved lab results.

If there is a section title, use:

Timeline

Subcopy:
Your confirmed lab results over time.

Do not use:
- Lab History
- Biomarker Timeline
- historical lab history
- Archive

7. Compact View Mode

The current View Mode section feels too large.

Change it to a compact single-line control:

View mode: [ Clinical Panels ] [ Signal Map ]

Visual:
- “View mode:” should be white/bold or semi-bold.
- The Clinical Panels / Signal Map controls should sit beside it if space allows.
- On small screens, allow wrapping gracefully.
- Do not create horizontal overflow.

8. Dynamic View Mode microcopy

Under the compact View Mode row, show microcopy based on selected mode.

If Clinical Panels selected:
Grouped by clinical lab panels.

If Signal Map selected:
Grouped by biological signal layers.

Keep the microcopy small, muted, and clean.

9. Preserve existing Current Markers logic

Do not change:
- which markers appear
- grouping
- sorting
- state badges
- range bars
- detail sheet behavior

Only adjust the header/control layout and copy.

10. Mobile polish

Ensure:
- no duplicated labels
- no excessive vertical space
- controls are easy to tap
- no horizontal overflow
- bottom nav does not cover content

11. Build and test

After editing:
1. Report exact file changed.
2. Confirm no unrelated files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload

Confirm:
- Labs header shows:
  Your clinical markers.
  Translated into biological signals.
- First line visually matches Home’s stronger subcopy style.
- Snapshot/Timeline control is no longer directly under the main Labs header.
- Snapshot section says Current Biomarker Snapshot.
- Snapshot / Timeline control lives under Current Biomarker Snapshot.
- timeframe says Current Year · [current year]
- View mode is compact:
  View mode: [ Clinical Panels ] [ Signal Map ]
- microcopy changes when toggling Clinical Panels / Signal Map
- Snapshot still works
- Timeline still works
- upload flow still works
- marker detail sheet still works
- build passes

Do not commit.
Do not push.
Do not publish.