# MERIDIAN — INSIGHT GUARDRAIL HARDENING PLAN ONLY

We are preparing Phase 2 of Meridian Insight Language Safety.

Important context:
The real app lives in:

.migration-backup/

This is PLAN ONLY.

Do NOT edit files.
Do NOT modify code.
Do NOT create files.
Do NOT add dependencies.
Do NOT commit.
Do NOT push.
Do NOT publish.

Background:
Insight Language Clarity Phase 1 is closed.

Known remaining risks:
1. Hallucination guard is soft:
   If the AI mentions a biomarker that is not present in the input JSON, the app currently appends a warning to logic_trace but still returns the insight.

2. Forbidden word guard currently replaces forbidden words with "***":
   This could render broken or confusing copy to the user if a forbidden word appears in headline/status/cause/action_steps/trust_line.

Goal:
Plan the safest Phase 2 hardening approach so Meridian never shows broken, hallucinated, diagnostic, or unsafe insight copy to users.

Inspect:

.migration-backup/src/app/api/insight/route.ts
.migration-backup/src/app/dashboard/page.tsx
any related fallback states in dashboard/intelligence flow if needed

Tasks:

1. Map current guardrail behavior

Explain exactly:
- where hallucination detection happens
- what happens when hallucination is detected
- where forbidden word detection happens
- what fields are scanned
- how words are replaced with "***"
- whether users can see "***"
- what response still gets returned to Dashboard
- how Dashboard renders fallback states

2. Recommend hallucination failure behavior

Decide the safest behavior if a generated insight mentions markers not present in input.

Evaluate options:
A. Return a safe fallback JSON insight
B. Return an insight_unavailable state
C. Retry once with stricter prompt
D. Suppress insight and show labs_saved fallback
E. Combination

Consider:
- user experience
- safety
- avoiding blank states
- avoiding scary language
- avoiding extra Claude cost
- keeping implementation simple

3. Recommend forbidden word failure behavior

Decide what should happen if forbidden words appear.

Evaluate:
A. Replace words with "***" as now
B. Retry once with stricter prompt
C. Return safe fallback insight
D. Return labs_saved / insight_unavailable fallback
E. Combination

Important:
Do not show "***" to the user.

4. Recommend safe fallback copy

If fallback JSON is recommended, propose copy that fits Meridian:

headline:
status:
cause:
action_steps:
trust_line:
block_color:

The fallback should:
- be calm
- not diagnose
- not mention invented markers
- not claim failure
- not show technical errors
- guide the user to review labs or try again later
- keep Meridian useful

5. Dashboard compatibility

Check whether Dashboard can handle:
- fallback JSON
- insight_unavailable
- labs_saved
- no_data
- safety_alert

Recommend the least risky integration path.

6. Retry strategy

Decide whether retry-once should be added now or later.

Consider:
- API cost
- latency
- complexity
- risk of repeated bad output
- whether fallback is safer for Phase 1

7. Implementation plan

Return a recommended Phase 2 implementation plan with:
- exact files to modify
- exact behavior changes
- expected output shape
- no schema change unless absolutely necessary
- no Dashboard redesign unless absolutely necessary

8. Risks

List risks of the proposed plan and mitigations.

9. Build/QA plan

Recommend QA cases:
- hallucinated marker simulation
- forbidden word simulation
- normal insight still passes
- safety alert still works
- Dashboard does not show "***"
- no blank states

Output required:

1. Executive recommendation
2. Current behavior map
3. Recommended hallucination guard behavior
4. Recommended forbidden word guard behavior
5. Safe fallback copy
6. Dashboard compatibility assessment
7. Retry strategy recommendation
8. Exact implementation plan
9. Risks and mitigations
10. QA plan
11. Confirmation:
   “No files were changed.”