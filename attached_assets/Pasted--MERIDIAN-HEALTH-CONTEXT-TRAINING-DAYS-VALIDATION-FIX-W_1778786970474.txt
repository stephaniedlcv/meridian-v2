# MERIDIAN — HEALTH CONTEXT TRAINING DAYS VALIDATION FIX

We found a small bug in Profile → Health Context.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:

.migration-backup/src/app/profile/page.tsx

Do not touch:
- dashboard
- labs
- upload flow
- OCR routes
- confirm route
- insight route
- intelligence route
- protocol
- onboarding
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

CURRENT BUG:
The Training days/week field says 1–7 or visually suggests a range.

The user entered something like:

4-5

because it looked like a range was acceptable.

The app accepted the input but saved it incorrectly as 0.

PRODUCT RULE:
training_days is a single integer field, not a range.

It should represent the user’s typical number of intentional training days per week.

Allowed values:
0, 1, 2, 3, 4, 5, 6, 7

Do NOT support ranges in this task.

TASK:
Fix the Training days/week input UX and validation.

REQUIREMENTS:

1. Make the helper copy clear

Change helper/copy near Training days/week to something like:

“Enter one number from 0 to 7. If your week varies, choose your usual average.”

Avoid wording that looks like a range.

Do not write:
“1–7”
by itself.

2. Use a safer input

If currently using a text input, change it to a number input if possible:

type="number"
min={0}
max={7}
step={1}
inputMode="numeric"

Still validate manually in saveHealthContext.

3. Strict validation

In saveHealthContext, training_days should save only if:
- input is not blank
- input is a finite number
- input is an integer
- input >= 0
- input <= 7

If input contains characters like:
- 4-5
- 3/4
- four
- 4.5

Do NOT save 0 by accident.

Instead:
- treat it as invalid
- set training_days to null OR keep the previous value if the existing save pattern supports preserving invalid fields safely

Preferred:
If invalid, do not silently convert to 0.
Show a small inline validation message:
“Use one whole number from 0 to 7.”

If adding inline validation is too much for this task, at minimum prevent saving invalid input as 0.

4. Do not break valid values

These should work:
0
1
2
3
4
5
6
7

5. Do not change schema

Do not change:
- training_days column type
- database types
- SQL
- other Health Context fields

6. Do not change unrelated Health Context fields

Do not change:
- height conversion
- weight conversion
- activity level
- body goal phase
- diet pattern

7. Build and QA

After editing, run:

cd .migration-backup && npm run build

QA checklist:
- Profile loads.
- Health Context edit opens.
- Training days helper clearly says one number from 0 to 7.
- Entering 4 saves 4.
- Entering 0 saves 0.
- Entering 7 saves 7.
- Entering 4-5 does not save as 0.
- Entering 4.5 does not save as 4 or 0.
- Invalid input either shows validation message or saves null, but never silently saves 0.
- Height still saves correctly.
- Weight still saves correctly.
- Activity level still saves correctly.
- Body goal phase still saves correctly.
- Diet pattern still saves correctly.
- Build passes.

Do not commit.
Do not push.
Do not publish.

OUTPUT REQUIRED:
Return:
1. Exact bug cause
2. Exact fix made
3. Exact file changed
4. Build result
5. QA checklist result
6. Confirmation no unrelated systems were touched