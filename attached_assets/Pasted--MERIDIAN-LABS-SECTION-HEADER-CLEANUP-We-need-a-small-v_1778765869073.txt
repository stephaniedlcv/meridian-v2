# MERIDIAN — LABS SECTION HEADER CLEANUP

We need a small visual hierarchy polish on the Labs page.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- dashboard
- profile
- protocol
- onboarding
- OCR routes
- confirm route
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create new files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Reduce visual overuse of pill/chip styling on the Labs page.

CURRENT ISSUE:
The page currently uses too many chip/pill treatments:
- BIOMARKER SIGNALS
- CURRENT BIOMARKER SNAPSHOT
- Snapshot | Timeline
- Optimal / Watch / Attention
- Clinical Panels | Signal Map

This makes the page feel slightly busy. We want the page to feel cleaner, more clinical, and more premium.

GOAL:
Keep the BIOMARKER SIGNALS chip as the only decorative/system identity chip in the header area.

Change CURRENT BIOMARKER SNAPSHOT from a pill/chip into a clean section label/header.

REQUIREMENTS:

1. Keep BIOMARKER SIGNALS as-is

Do not change the top chip:
BIOMARKER SIGNALS

That remains the module identity badge.

2. Change CURRENT BIOMARKER SNAPSHOT styling

Currently it appears inside a pill/chip.

Remove the pill/chip container styling.

Render it as a clean section label similar to CURRENT MARKERS:
- uppercase
- small size
- letter spacing
- muted teal color
- no border
- no rounded pill background
- no glowing chip treatment

Text should remain:
CURRENT BIOMARKER SNAPSHOT

3. Keep Snapshot | Timeline segmented control

Do not remove or rename the Snapshot | Timeline segmented control.

Keep it as an actual control.

4. Move Current Year under the toggle

Place:

Current Year · [current year]

under the Snapshot | Timeline toggle, left-aligned.

Do not hardcode the year.

5. Desired structure

The Snapshot section should read visually as:

CURRENT BIOMARKER SNAPSHOT

[ Snapshot | Timeline ]

Current Year · 2026

[summary card]

[status chips]

6. Do not change data logic

Do not change:
- upload flow
- Timeline data
- Snapshot data
- Clinical Panels / Signal Map logic
- status counts
- range bars
- marker detail sheet
- history/timeline grouping

This is visual layout polish only.

7. Build and test

After editing:
1. Report exact file changed.
2. Confirm no unrelated files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload

Confirm:
- BIOMARKER SIGNALS chip remains unchanged
- CURRENT BIOMARKER SNAPSHOT is no longer inside a chip/pill
- Snapshot | Timeline still works
- Current Year · [current year] appears under the toggle
- Snapshot still works
- Timeline still works
- Clinical Panels / Signal Map still works
- upload flow still works
- build passes

Do not commit.
Do not push.
Do not publish.