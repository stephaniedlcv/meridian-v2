# MERIDIAN — AI COLLABORATION PROTOCOL v2

You are a builder on Meridian — a Health Intelligence System.
Stack: Next.js 14 App Router · Supabase · TypeScript · Vercel

## CRITICAL RULES
- Modify ONLY the file(s) listed below unless absolutely necessary.
- Do NOT create unnecessary files.
- Do NOT add dependencies.
- Do NOT use Tailwind classes. Inline styles ONLY.
- Do NOT redesign the global Meridian design system.
- Preserve the existing visual language: premium dark glassmorphism, teal/cyan accents, Fraunces for major headings, Plus Jakarta Sans for UI/body.
- Keep bottom NavBar working and visible.
- TypeScript strict.
- Return COMPLETE files, not snippets.

---

## TASK
Complete the /profile page and turn it into a futuristic Meridian profile experience.

The current /profile page is only a placeholder. Replace it with a premium, futuristic profile page that feels different from a normal generic settings screen.

## GOAL
Create a “Profile / Identity / Health Control Center” experience.

The page should:
- feel futuristic and premium
- support a user photo
- support a preferred name / display name
- support health identity fields already relevant to Meridian
- match the Meridian dark glassmorphism design system
- feel warm, elevated, and habit-forming — something users want to revisit

---

## UX REQUIREMENTS

### 1. Hero section
At the top of the page, create a premium hero profile block with:
- large profile photo / avatar
- preferred/display name as the main heading
- email below in smaller muted text
- a small label/chip like “Your Meridian Profile” or “Biological Identity”
- subtle ambient orb/glow treatment in the background

If the user has no uploaded photo:
- show a premium placeholder avatar (not boring initials-only if possible)
- still show an “Upload photo” action

### 2. Photo actions
Support these actions in the UI:
- Upload photo
- Change photo
- Remove photo

If storage/backend support is not already available:
- still build the UI structure cleanly
- if possible, use a temporary local preview flow
- if profile avatar_url or photo_url exists, use it
- if it does not exist yet, clearly isolate the logic so it can be wired later

### 3. Identity card
Create a glass card for identity info:
- Preferred name / Display name
- Email
- Biological profile
- User profile / health focus mode

Use a layout that feels editorial and premium, not like a boring form.

### 4. Health focus card
Create a card that shows the user’s current Meridian orientation:
- bienestar
- optimizacion
- rendimiento
- condicion
- primer_paso

Use soft educational microcopy to explain that this helps Meridian personalize insights.

### 5. Connected sources card
Create a card for data sources:
- Labs
- Wearables
- Other future sources if needed

Each source should show a visual status:
- Connected
- Not connected
- Coming soon

This can be read-only for now, but should look intentional and polished.

### 6. Preferences / account card
Create a card for:
- Edit profile
- App preferences (placeholder if necessary)
- Sign out

Keep this clean and minimal.

---

## DATA / LOGIC REQUIREMENTS

### Auth
Use the same browser auth guard pattern used in dashboard/history/upload/profile:
- createBrowserClient
- supabase.auth.getUser()
- redirect unauthenticated users to /onboarding/welcome (or the existing auth entry point used in the app)

### Profile data
Read profile data from the profiles table.

Use these values if available:
- full_name
- preferred_name (if it exists)
- biological_profile
- user_profile
- email from auth user

IMPORTANT:
- Never use the email prefix as the user’s long-term display name strategy.
- For this page, use:
  1. preferred_name if available
  2. full_name if available
  3. fallback to a warm generic fallback like “Your Profile”

If the database does NOT yet contain preferred_name:
- keep the logic clean and future-ready
- do not break existing behavior

### Edit mode
Add a clean inline edit flow or a small edit mode for the core profile fields:
- preferred/display name
- biological profile
- user profile

If saving is implemented:
- use Supabase update on the profiles table
- show loading/saving state
- show success/error feedback in Meridian style

If a field does not yet exist in the DB:
- do not invent breaking schema assumptions
- structure the code so it is easy to wire later

---

## VISUAL REQUIREMENTS

### General style
- Dark premium background
- Ambient teal/cyan orbs
- Glass cards with subtle borders
- Fraunces for key headings
- Plus Jakarta Sans for body/UI
- Avoid generic “settings app” vibes
- Should feel like Meridian is showing the user’s biological identity

### Profile photo styling
- modern circular or softly rounded treatment
- elegant border/glow
- premium look

### Cards
Cards should feel modular and futuristic:
- soft glass blur
- teal outlines or subtle highlights
- excellent spacing
- mobile-first layout

### Copy tone
Warm, intelligent, futuristic, and clean.
Not clinical in a cold way.
Not cheesy.

---

## FILES TO MODIFY
- /src/app/profile/page.tsx

If absolutely necessary and already existing in the project, you may also modify:
- any directly related local helper already used by this page
But avoid changing anything else unless required.

---

## OUTPUT
Return the complete updated file(s) only.