# MERIDIAN — TASK 4: Labs Snapshot Reliability + Status Filters

You are working on Meridian.

Stack:
- Next.js 14 App Router
- Supabase
- TypeScript
- Vercel

IMPORTANT:
- The real app lives inside .migration-backup/
- Do NOT move files
- Do NOT restructure repo
- Do NOT redesign Labs
- Do NOT add dependencies
- Do NOT touch artifacts/
- Do NOT change History archive behavior

This task is ONLY for the Labs snapshot page.

---

# GOAL

Make the Labs page behave like a clean current-state snapshot, not a full archive.

History = full lab archive  
Labs = recent/current biomarker snapshot

---

# REQUIRED FIXES

## 1. Latest unique biomarkers

Labs snapshot should show the latest unique biomarker results only.

Deduplicate by:
- marker_slug when available
- fallback to normalized marker_name

Use the most recent collected_at result for each marker.

Do NOT show repeated copies of the same biomarker in the Labs snapshot.

---

## 2. Last 12 months only

Labs snapshot should only include biomarkers from the last 12 months.

History should continue showing all saved data.

Do NOT change History behavior.

---

## 3. Correct counts

The snapshot count should reflect latest unique biomarkers, not total database rows.

Update:
- biomarker count
- panel count
- Optimal count
- Watch count
- Attention count
- Critical count if supported

Counts should match what the snapshot is actually showing.

---

## 4. Needs Attention section

The Needs Attention section should:
- show only latest unique biomarkers
- avoid duplicates
- include Watch, Attention, and Critical if applicable
- prioritize highest severity first:
  1. Critical
  2. Attention
  3. Watch

If no biomarkers need attention, show a calm empty state.

---

# 5. Status filter chips

The status summary chips should be interactive filters.

Current chips:
- Optimal
- Watch
- Attention

If Critical exists, include Critical.

Expected behavior:

- Tap Optimal → show only Optimal biomarkers
- Tap Watch → show only Watch biomarkers
- Tap Attention → show only Attention biomarkers
- Tap Critical → show only Critical biomarkers
- Tap active chip again → clear filter and return to all snapshot biomarkers

The active chip should have a clear selected state.

Do NOT redesign the chips.  
Use the existing visual language.

---

# 6. Filtered list behavior

When a status filter is active:

- show only biomarkers matching that status
- update section heading clearly, for example:
  - Optimal Biomarkers
  - Watch Biomarkers
  - Attention Biomarkers
  - Critical Biomarkers

If no biomarkers match the selected filter:
show a calm empty state, for example:

No biomarkers in this status for your recent snapshot.

---

# 7. Preserve biomarker detail behavior

Filtered biomarker cards must still:
- open the existing biomarker detail sheet
- show range bar when valid range exists
- show clear no-range message when no range exists
- not invent ranges

---

# DO NOT CHANGE

Do NOT:
- change History archive
- implement biological/source toggle
- add new AI interpretation
- change upload flow
- change parser logic
- change onboarding
- redesign card UI
- change status calculation logic

---

# ACCEPTANCE CRITERIA

After this task:

1. Labs page shows latest unique biomarkers only.
2. Snapshot count does not inflate from duplicate uploads.
3. Needs Attention does not show duplicates.
4. Status counts match the rendered snapshot data.
5. Tapping Optimal filters to Optimal biomarkers only.
6. Tapping Watch filters to Watch biomarkers only.
7. Tapping Attention filters to Attention biomarkers only.
8. Tapping active chip again clears the filter.
9. Empty filter results show a calm empty state.
10. History still shows full archive unchanged.

---

# RETURN FORMAT

Return:
1. Files changed
2. Deduplication logic implemented
3. Status filter behavior implemented
4. Counts logic updated
5. Any remaining edge cases
6. Confirmation no files outside .migration-backup/ were modified