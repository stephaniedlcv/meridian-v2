# MERIDIAN — INSIGHT LANGUAGE PHASE 1C PLAIN LANGUAGE POLISH

We tested the updated insight language in production.

The improvement is significant, but some residual wording is still too technical or ambiguous.

Important context:
The real app lives in:

.migration-backup/

Modify ONLY:
.migration-backup/src/app/api/insight/route.ts

Do not touch:
- dashboard
- labs
- profile
- onboarding
- upload flow
- OCR routes
- confirm route
- protocol
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create files.
TypeScript strict.

CURRENT OUTPUT EXAMPLES:
Status:
“eGFR at 84 may reflect hydration, recent intake, or workload today.”

Cause:
“This signal may reflect temporary shifts in fluid balance or recent protein intake…”

These are safer than before, but still not simple enough.

ISSUE:
- “workload” is ambiguous.
- “fluid balance” is a little technical.
- Meridian should use plain everyday language first.

TASK:
Add a plain-language polish rule to the insight system prompt.

REQUIREMENTS:

1. Add plain-language rule

Add a new absolute rule:

Use everyday language first. If a technical phrase is necessary, explain it in simple words. The user should not need medical knowledge to understand the insight.

2. Discourage residual vague/technical terms

Discourage these words/phrases in user-facing copy unless clearly explained:

- workload
- fluid balance
- metabolic demand
- filtration efficiency
- renal
- kidney workload
- systemic stress
- biomarker instability

3. Preferred replacements

Add preferred replacements:

Instead of:
“workload”

Use:
“recent activity”
“recent training”
“recent physical stress”
“recent demand on your body”

Instead of:
“fluid balance”

Use:
“hydration”
“how much fluid your body is holding”
“fluid shifts”

Instead of:
“filtration efficiency”

Use:
“filtration marker”
“kidney-related marker”
“this lab signal”

Instead of:
“metabolic demand”

Use:
“recent activity”
“recent training”
“your body’s recent workload” only if explained simply

4. Add examples

Bad:
“eGFR at 84 may reflect hydration, recent intake, or workload today.”

Good:
“eGFR at 84 may reflect hydration, recent meals, or recent activity today.”

Bad:
“This signal may reflect temporary shifts in fluid balance or recent protein intake.”

Good:
“This signal may reflect temporary shifts in hydration or recent protein intake.”

Bad:
“Filtration efficiency is worth watching.”

Good:
“This filtration marker is worth watching alongside your other labs.”

5. Keep existing rules

Do not remove:
- Mom Test
- vague standalone phrase ban
- organ-language caution
- headline caution
- supplement safety rule
- hallucination guard
- forbidden word guard

6. Do not change schema or Dashboard

Do NOT add fields.
Do NOT change Dashboard.
Do NOT add Health Context yet.

7. Build and QA

Run:

cd .migration-backup && npm run build

QA checklist:
- Build passes.
- Prompt discourages “workload” unless explained.
- Prompt discourages “fluid balance” unless explained.
- Preferred plain-language replacements are present.
- Examples are present.
- Existing action step clarity rules remain.
- Existing guardrails remain.
- JSON schema unchanged.
- Dashboard unchanged.
- Health Context still not added.

Do not commit.
Do not push.
Do not publish.

OUTPUT REQUIRED:
Return:
1. Exact language rule added
2. Exact file changed
3. Build result
4. QA checklist result
5. Confirmation no unrelated systems were touched