# MERIDIAN — INSIGHT BOUNDARY NOTE PHASE A

We are implementing the next low-risk phase of Meridian Insight Language Safety.

Important context:
The real app lives in:

.migration-backup/

Goal:
Add a deterministic, low-emphasis boundary note to solved insights so users understand what not to overinterpret.

This should reduce anxiety and clarify that Meridian gives directional signals, not diagnoses.

Implementation decision:
Use deterministic Dashboard copy.

Do NOT add a new AI-generated field.
Do NOT change the insight JSON schema.
Do NOT update Claude prompt.
Do NOT touch insight/route.ts.
Do NOT touch Supabase or database types.

Modify ONLY:
.migration-backup/src/app/dashboard/page.tsx

Do not touch:
- insight API route
- intelligence route
- labs
- profile
- onboarding
- upload flow
- OCR routes
- confirm route
- protocol
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- dependencies
- artifacts/

Do not add dependencies.
Do not create files.
Do not use Tailwind.
Inline styles only.
TypeScript strict.

TASK:
Add a deterministic boundary note inside SolvedBlock.

CURRENT SolvedBlock layout:
- headline
- status
- cause
- Today's Priority
- action step cards
- separator
- Confidence Trace
- trust_line

NEW layout:
- headline
- status
- cause
- Today's Priority
- action step cards
- boundary note
- separator
- Confidence Trace
- trust_line

REQUIREMENTS:

1. Boundary copy selection

Add deterministic copy based on block_color and safetyAlert.

Use:

For block_color === 'optimal':
"This is a directional signal, not a conclusion. Patterns over time are more meaningful than any single reading."

For block_color === 'recovery':
"This is a signal to watch, not a diagnosis. One reading reflects a moment — context, hydration, and recent activity all matter."

For block_color === 'alert' OR safetyAlert === true:
"This result is worth reviewing with a qualified clinician before drawing conclusions. Meridian interprets data; it does not diagnose."

Default fallback:
Use the recovery copy.

2. Placement

Render the boundary note:
- below the action step cards
- above the existing Confidence Trace separator

Do not move the trust_line.
Do not move action steps.
Do not redesign the block.

3. Visual treatment

Boundary note should be visible but low-emphasis:
- fontSize: 12px
- color: colors.textMuted
- lineHeight: 1.55
- marginTop: 16px
- fontStyle: italic
- no icon
- no card
- no border
- no warning styling

It should not compete with the action steps.

4. Label

Use a tiny label above the boundary copy:

Keep in mind

Style:
- uppercase
- fontSize: 10px or 11px
- letterSpacing similar to other micro-labels
- color: colors.textMuted
- marginBottom: 4px

If adding the label makes the block feel too busy, keep only the sentence. Choose the cleaner option based on existing SolvedBlock style.

5. Safety behavior

If safetyAlert === true, always use the alert/professional-review copy even if block_color is not 'alert'.

6. Do not change schema

Do NOT add:
- boundary
- context_note
- any new field to GoldenInsight

Do NOT change API response shape.

7. Do not change guardrails

Do not change:
- hallucination guard
- forbidden word guard
- action step rules
- Claude prompt
- Decision Engine
- Safety Engine thresholds

8. Build and QA

Run:

cd .migration-backup && npm run build

QA checklist:
- Build passes.
- Dashboard compiles.
- SolvedBlock renders boundary note under action steps.
- Boundary note appears before Confidence Trace.
- trust_line still renders.
- action steps remain visually primary.
- optimal block uses optimal copy.
- recovery block uses recovery copy.
- alert/safety block uses professional-review copy.
- No API route files changed.
- No schema changes.
- No Profile/Labs files changed.

Do not commit.
Do not push.
Do not publish.

OUTPUT REQUIRED:
Return:
1. Exact change made
2. Exact file changed
3. Whether API/schema were untouched
4. Build result
5. QA checklist result
6. Confirmation no unrelated systems were touched