Audit and repair the Meridian notifications system logic end-to-end.

IMPORTANT:
- Preserve current UI styling
- Preserve current notification architecture
- This is a backend + state synchronization repair pass

CURRENT ISSUES

1. Notifications remain unread even after viewing them
2. Notification badge/dot does not clear correctly
3. Some targeted users are not receiving notifications
4. Notification delivery state is unclear
5. Admin has no visibility into delivery/open state

━━━━━━━━━━━━━━━━━━━━
TASK 1 — FIX READ STATE SYNCHRONIZATION
━━━━━━━━━━━━━━━━━━━━

When a user:
- opens the notifications inbox
OR
- taps a notification

the notification must correctly persist as read/opened in Supabase.

Ensure:
- opened = true
- opened_at timestamp saved
- unread badge updates correctly
- notification bell state syncs immediately
- state survives refresh/re-login

Audit:
- frontend state
- API routes
- Supabase mutations
- optimistic updates
- query invalidation

━━━━━━━━━━━━━━━━━━━━
TASK 2 — FIX NOTIFICATION DELIVERY TARGETING
━━━━━━━━━━━━━━━━━━━━

Audit the notification targeting system.

Verify:
- notifications table creation
- notification_recipients row generation
- segmentation queries
- profile joins
- biological_profile filtering
- specific-user targeting
- all-users targeting

Ensure:
- male/female targeting works correctly
- specific user selection works correctly
- recipient rows are always generated
- notifications appear for intended recipients

━━━━━━━━━━━━━━━━━━━━
TASK 3 — ADD DELIVERY DIAGNOSTICS
━━━━━━━━━━━━━━━━━━━━

In Admin Notifications:
add visibility for:
- recipient count
- delivered count
- opened count
- failed count (if applicable)

Also show:
- draft
- scheduled
- sent
- archived

━━━━━━━━━━━━━━━━━━━━
TASK 4 — ADD RECIPIENT PREVIEW BEFORE SEND
━━━━━━━━━━━━━━━━━━━━

Before sending notifications:
show a preview such as:

This notification will reach:
- 4 users
- 3 female
- 1 male

This prevents silent targeting failures.

━━━━━━━━━━━━━━━━━━━━
TASK 5 — PRESERVE CURRENT UX
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- redesign notifications UI
- change visual language
- remove existing features

This is:
logic repair + delivery reliability + state synchronization.

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Return:
- exact files modified
- complete code
- no pseudo-code
- no explanations unless necessary