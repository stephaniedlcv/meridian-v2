The “Upload another PDF” bug is still happening in production.

Current behavior:
After confirming a lab, I click the option to upload another lab/PDF, but the app still sends me back to Home/Dashboard instead of staying on /labs/upload.

Previous attempted fix:
A timeout ref was added to cancel the auto-redirect timer when handleReset runs.

Issue:
The bug is still happening, so either:
1. The button is not calling handleReset,
2. Another redirect timer is still active,
3. The success action uses router.push somewhere else,
4. The deployed code differs from the local code,
5. Or the auto-redirect should be disabled when the user explicitly clicks “Upload another”.

Modify ONLY this file if needed:

.migration-backup/src/app/labs/upload/page.tsx

Do not touch:
- OCR routes
- confirm route
- dashboard
- history
- canonical dictionary
- Supabase clients
- database types
- middleware
- onboarding
- NavBar
- package.json
- artifacts/

First diagnose:
1. Inspect the success state UI.
2. Identify the exact button/link text for “Upload another PDF”.
3. Confirm which handler it calls.
4. Confirm whether handleReset cancels the auto-redirect timer.
5. Confirm whether any other router.push('/dashboard') or router.push('/') exists in this file.
6. Confirm whether there is any useEffect that redirects after success.

Then implement the smallest safe fix:
- “Upload another PDF” must cancel any pending redirect.
- It must reset the upload state.
- It must stay on /labs/upload.
- It must show the file picker again.
- It must not navigate to / or /dashboard.
- “Back to home” or “View dashboard” may still route to /dashboard.

After editing:
1. Report exact file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /labs/upload
5. Confirm build passes.
6. Explain exactly why clicking “Upload another PDF” now cannot redirect to dashboard.
7. Do not commit.
8. Do not push.
9. Do not publish.