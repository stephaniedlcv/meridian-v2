STOP before making more isolated visual changes.

We need to enforce visual consistency across the onboarding flow.

Modify ONLY these files:

1. .migration-backup/src/app/onboarding/goals/page.tsx
2. .migration-backup/src/app/onboarding/connect/page.tsx

Use these screens as the absolute visual source of truth:
- /
- /onboarding/welcome

Goal:
Make /onboarding/goals and /onboarding/connect look like they belong to the exact same product system as the upgraded landing and welcome/auth screens.

The background, typography, colors, glow intensity, card treatment, CTA styling, and overall visual language must feel continuous across:
- /
- /onboarding/welcome
- /onboarding/goals
- /onboarding/connect

Current issue:
The screens do not feel fully consistent:
- /goals content is left-aligned inside a glass card
- /connect content is centered and floating
- colors feel slightly different
- spacing and layout structure are not identical
- chip treatment is missing from connect
- connect does not feel like a continuation of goals or welcome

Required global consistency rules:

1. Same page structure
Both screens must use the same structure:
- centered page container
- step chip above the card
- main glass card/container
- title inside the card
- subtitle under title
- content grid/list
- CTA button
- secondary/helper text

2. Same background system
Use the same background feeling as / and /onboarding/welcome:
- same dark base
- same soft teal/cyan atmospheric glow language
- same subtle ambient orb treatment
- same calm futuristic biological OS feel
- no brighter or harsher variants

3. Same alignment
Use the same alignment across both screens.

Preferred:
- Main card content should be left-aligned, like /onboarding/goals.
- The outer page/container can remain centered.
- Do not have /goals left-aligned and /connect center-aligned.
- Make them feel like consecutive steps in one flow.

4. Same card system
Both screens should use the same main glass card style:
- same maxWidth
- same background
- same border
- same borderRadius
- same boxShadow / glow
- same padding
- same inner top-edge highlight

Use /onboarding/welcome and /onboarding/goals as the base visual reference.

5. Same typography
All titles across these two screens must use:
- fontFamily: "var(--font-fraunces), serif"
- same fontSize
- same fontWeight
- same lineHeight
- same letterSpacing
- same color

Titles:
- “What's your health goal?”
- “Connect your data”

Both must stay on one line on desktop when possible.

6. Same subtitle/body typography
Subtitles and helper text should use the same:
- font family
- color
- size
- lineHeight
- opacity
- spacing

7. Same color system
Use the same Meridian tokens consistently:
- background: #061316
- teal: #2DD4BF
- cyan: #67E8F9
- text: #EAFBF7
- textSoft: #9ACBC1
- textMuted: #5F8E85
- card border: rgba(103,232,249,0.13)
- card background: rgba(232,248,245,0.055)

Do not introduce new teal/cyan variants unless already used in the approved landing/welcome screens.

8. Same step chip system
Both screens need matching chips:
- /goals: EXTENDED PROFILE · STEP 2
- /connect: CONNECT DATA · STEP 3

The chip style must be identical:
- same fontSize
- same letterSpacing
- same padding
- same border
- same glow/dot
- same color
- same margin spacing

9. Same option card system
Goal cards and connect option cards should feel related:
- same borderRadius language
- same background tone
- same border opacity
- same selected state glow
- same hover/active style if present
- same text hierarchy

10. Same CTA system
Continue buttons should match:
- same height
- same borderRadius
- same gradient
- same fontWeight
- same glow
- same width behavior
- same spacing above/below

11. Preserve all logic
Do not change:
- Supabase calls
- auth guards
- handleContinue
- handleSkip
- completeOnboarding
- router.push
- field names
- state names
- validation logic
- selection behavior

Visual-only edits.

Important:
This is a coherence pass, not a redesign.
The goal is to make the onboarding flow feel like one unified premium biological intelligence OS.

After editing:
1. Report every file changed.
2. Confirm no other files changed.
3. Summarize exactly how goals and connect were made consistent:
   - layout
   - alignment
   - typography
   - colors
   - background
   - card system
   - chip system
   - CTA system
4. Run:
   cd .migration-backup && npm run build
5. Test:
   - /onboarding/goals
   - /onboarding/connect
6. Confirm build passes.
7. Do not commit.
8. Do not push.
9. Do not publish to Replit.