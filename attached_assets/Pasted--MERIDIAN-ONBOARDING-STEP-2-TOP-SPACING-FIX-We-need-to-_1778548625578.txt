# MERIDIAN — ONBOARDING STEP 2 TOP SPACING FIX

We need to fix a visual spacing issue on the onboarding Biological Profile screen.

Important context:
The real app lives in:

.migration-backup/

Do NOT restructure the repo.
Do NOT touch artifacts.
Do NOT publish to Replit.

Current issue:
On /onboarding/profile, there is too much empty vertical space at the top before the step chip:

BIOLOGICAL PROFILE · STEP 2

The chip/card start too low on mobile, making the page feel inconsistent with the other onboarding steps.

Goal:
Make /onboarding/profile top spacing visually consistent with /onboarding/identity and /onboarding/goals.

Modify ONLY:

.migration-backup/src/app/onboarding/profile/page.tsx

Do not touch:
- onboarding/identity
- onboarding/goals
- dashboard
- labs
- history
- profile page outside onboarding
- OCR routes
- confirm route
- canonical dictionary
- Supabase clients
- database types
- middleware
- NavBar
- package.json
- artifacts/
- dependencies

Requirements:
1. Reduce the excessive top empty space.
2. The step chip should sit closer to the top, consistent with the other onboarding screens.
3. The main card should remain comfortably spaced below the chip.
4. Do not make it cramped.
5. Do not redesign the screen.
6. Keep the current visual style:
   - dark glassmorphism
   - teal/cyan
   - premium mobile-first
   - same card styling
   - same typography
7. Check if the issue is caused by:
   - paddingTop
   - marginTop
   - minHeight
   - justifyContent: center
   - alignItems/centered vertical layout
8. If the page uses vertical centering, adjust it so mobile starts higher instead of centering the whole card too low.
9. Keep the Continue button and selection behavior unchanged.
10. Inline styles only.
11. No new files.
12. No dependencies.

Suggested direction:
- main wrapper paddingTop around 72px–96px on mobile, not excessive
- avoid full vertical centering if it creates a large blank top area
- keep enough bottom padding for Safari mobile browser UI

After editing:
1. Report exact file changed.
2. Confirm no other files changed.
3. Run:
   cd .migration-backup && npm run build
4. Test:
   /onboarding/profile
5. Confirm:
   - top spacing is reduced
   - Step 2 visually aligns better with Step 1 and Step 3
   - Continue still works
   - build passes
6. Do not commit.
7. Do not push.
8. Do not publish.